---
phase: 15-new-screens-core-flow
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/PracticeSession.tsx
  - src/components/BottomControlBar.tsx
  - src/components/CountdownOverlay.tsx
autonomous: true

must_haves:
  truths:
    - "3-second countdown animation plays before recording begins"
    - "SessionOrb dominates center of screen during recording"
    - "Practice prompt is persistent but dimmed at top — glanceable, doesn't compete with orb"
    - "Real-time metrics (WPM + filler count) shown in thin bottom strip during recording"
    - "Stop button only — no pause button during recording"
    - "Progress bar at top shows time remaining (existing pattern preserved)"
  artifacts:
    - path: "src/components/CountdownOverlay.tsx"
      provides: "3-second countdown animation overlay before recording starts"
      min_lines: 40
    - path: "src/components/PracticeSession.tsx"
      provides: "Redesigned recording screen with countdown, metrics strip, stop-only controls"
      min_lines: 200
  key_links:
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/CountdownOverlay.tsx"
      via: "render before recording starts"
      pattern: "import.*CountdownOverlay"
    - from: "src/components/PracticeSession.tsx"
      to: "src/components/SessionOrb.tsx"
      via: "center stage during recording"
      pattern: "import.*SessionOrb"
---

<objective>
Redesign the recording screen with a 3-second countdown, stop-only controls, dimmed practice prompt, and a minimal real-time metrics strip.

Purpose: SCR-03 and SCR-04 define the core recording experience. The orb should dominate, metrics should be minimal, and the flow should feel like a continuous real conversation (no pause button). The countdown builds anticipation before recording starts.

Output: New CountdownOverlay.tsx, redesigned PracticeSession.tsx, simplified BottomControlBar.tsx.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-new-screens-core-flow/15-CONTEXT.md
@.planning/phases/15-new-screens-core-flow/15-01-SUMMARY.md
@src/components/PracticeSession.tsx
@src/components/SessionOrb.tsx
@src/components/BottomControlBar.tsx
@src/components/SessionProgressBar.tsx
@src/components/AudioQualityWarning.tsx
@src/components/SilenceNudge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CountdownOverlay component</name>
  <files>src/components/CountdownOverlay.tsx</files>
  <action>
Create a full-screen countdown overlay that plays a 3-2-1 animation before recording begins. Per user decision (LOCKED): "3-second countdown animation before recording begins."

**Props:**
```tsx
interface CountdownOverlayProps {
  onComplete: () => void;  // Called when countdown finishes
}
```

**Visual design:**
- Full-screen overlay on dark background (`bg-background`)
- Large centered number: 3 → 2 → 1 with scale + fade animation
- Number styling: `text-display` or larger (Claude's discretion), `font-display`, `text-text-heading`
- Each number scales up from 0.5 to 1.0 and fades in/out over ~800ms
- After "1" fades, brief 200ms pause then call `onComplete`
- Subtle gold accent ring or glow around number (optional, Claude's discretion)

**Implementation:**
- Use useState for current count (3, 2, 1)
- useEffect with setInterval (1000ms) to decrement
- CSS animation classes for scale+fade transitions
- When count reaches 0, call onComplete after brief delay
- Component unmounts after complete

**No audio/sound** — purely visual countdown.
  </action>
  <verify>Build passes: `npm run build`. CountdownOverlay.tsx exists and exports the component.</verify>
  <done>CountdownOverlay displays animated 3-2-1 countdown with scale+fade transitions and calls onComplete when finished.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign PracticeSession recording experience</name>
  <files>src/components/PracticeSession.tsx, src/components/BottomControlBar.tsx</files>
  <action>
Redesign the PracticeSession.tsx recording screen per user decisions (LOCKED):

**1. Pre-recording state changes:**
- Remove the pre-recording idle state with "Tap to start" (user now comes from PreSessionScreen)
- When component mounts, show CountdownOverlay first
- After countdown completes, auto-start the recording (call handleStart)
- Add state: `showCountdown` (starts true, set to false after countdown)
- During countdown, show the orb in idle state behind the overlay

**2. Recording layout restructure:**
- **Top:** SessionProgressBar (keep existing — progress bar at top for time remaining)
- **Practice prompt** — persistent at top below progress bar, BUT dimmed:
  - Show technique prompt or speaking prompt in `text-body-sm text-text-subtle` (dimmed, not prominent)
  - Max 2 lines, truncated with ellipsis
  - Glanceable but doesn't compete with orb
- **Center:** SessionOrb — large, dominant, centered vertically
  - Remove all other center-stage elements that competed with orb
  - Orb should have maximum visual real estate
- **Bottom strip:** Real-time metrics in thin bar:
  - Left: filler count (e.g., "3 fillers") — `text-body-sm text-text-muted`
  - Right: WPM (e.g., "142 WPM") — `text-body-sm text-text-muted`
  - Thin strip: `py-2`, minimal height, subtle `border-t border-background-elevated`
  - Only show during active recording (not during countdown)

**3. Stop-only controls (LOCKED: "Stop only — no pause button"):**
- Remove pause functionality from the recording screen
- BottomControlBar: simplify to show ONLY stop button during recording
- Stop button: can be the orb itself (tap orb to stop — already implemented via square overlay in Phase 13), OR a separate button below the metrics strip
- Per Phase 13: "Stop = square overlay on orb (already implemented)" — keep this pattern
- Remove the pause/continue flow from BottomControlBar
- BottomControlBar during recording: just show a subtle "Tap orb to stop" hint text, or nothing at all (orb handles stop via onClick)
- Actually, to make stop discoverable: show a small "End Session" text button below the metrics strip, styled subtly (`text-body-sm text-text-subtle`)

**4. BottomControlBar simplification:**
- Remove the `paused` state handling entirely
- Recording state: remove Pause button, just show subtle end session option
- Remove `onPause` and `onContinue` props
- Keep `onStop` prop
- Simplify to a minimal layout

**5. Preserve all existing logic:**
- Audio capture, speech recognition, filler detection, WPM calculation — all stay
- Silence detection, nudge — keep working
- Audio quality warnings — keep (already subdued from Phase 14)
- Session end processing (blob to base64, sessionStorage, navigation) — all stays
- Baseline prompts — keep working
- The handlePause/handleContinue functions can remain in the code (they just won't be called from UI)

**6. Immersive mode:**
- No AppHeader during recording (already immersive from Phase 14)
- Full screen devoted to orb + minimal metrics
- Back button removed during active recording (can't leave mid-session)

**Important:** Keep the `isCapturing` check in the existing pre-recording state. The flow is now:
1. Component mounts → show CountdownOverlay (with idle orb behind)
2. Countdown completes → auto-start recording
3. Recording active → orb animates, metrics strip visible, stop available
4. Stop → process results → navigate to post-session
  </action>
  <verify>Build passes: `npm run build`. PracticeSession shows countdown before recording. During recording: orb center, dimmed prompt top, metrics strip bottom, stop-only control. No pause button visible.</verify>
  <done>Recording screen shows 3-second countdown, then SessionOrb dominates center with dimmed prompt at top and WPM+filler metrics in bottom strip. Stop-only control (no pause). Progress bar at top shows time remaining.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. 3-second countdown animation plays before recording starts
3. SessionOrb is center stage during recording
4. Practice prompt is visible but dimmed at top
5. WPM and filler count shown in thin bottom strip
6. No pause button — stop only
7. Progress bar at top shows remaining time
8. All audio/speech/filler logic still works correctly
9. Session end navigation still works (results page)
</verification>

<success_criteria>
- Countdown overlay plays 3-2-1 animation before recording
- Recording screen is immersive with orb dominating
- Metrics strip is minimal and non-distracting
- No pause button — sessions are continuous
- All existing audio/speech functionality preserved
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-new-screens-core-flow/15-04-SUMMARY.md`
</output>
