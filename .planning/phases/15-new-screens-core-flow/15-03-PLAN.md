---
phase: 15-new-screens-core-flow
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/pages/PreSessionScreen.tsx
autonomous: true

must_haves:
  truths:
    - "Technique practice shows briefing card with technique name, what to practice, and success criteria"
    - "AI persona displayed as name + style label only (no avatar art)"
    - "Duration selector appears below the briefing card"
    - "Free practice mode shows simplified view: speaking prompt selector + duration only"
    - "Single CTA button at bottom starts the session"
    - "Screen uses AppHeader with back arrow mode"
  artifacts:
    - path: "src/pages/PreSessionScreen.tsx"
      provides: "Redesigned pre-session screen with technique briefing and free practice modes"
      min_lines: 100
  key_links:
    - from: "src/pages/PreSessionScreen.tsx"
      to: "src/data/techniques.ts"
      via: "getTechniqueById to load technique briefing data"
      pattern: "import.*getTechniqueById"
    - from: "src/pages/PreSessionScreen.tsx"
      to: "src/components/AppHeader.tsx"
      via: "import and render with showBack=true"
      pattern: "import.*AppHeader"
    - from: "src/pages/PreSessionScreen.tsx"
      to: "src/components/DurationSelector.tsx"
      via: "import for duration selection"
      pattern: "import.*DurationSelector"
---

<objective>
Redesign the Pre-Session screen to support both technique practice (with briefing card) and free practice modes with the dark premium design system.

Purpose: SCR-02 requires a pre-session screen that briefs users on what they're about to practice (techniques) or lets them quickly start free practice. This screen bridges technique selection and the recording experience.

Output: Redesigned PreSessionScreen.tsx with technique briefing and free practice modes.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-new-screens-core-flow/15-CONTEXT.md
@.planning/phases/15-new-screens-core-flow/15-01-SUMMARY.md
@src/pages/PreSessionScreen.tsx
@src/pages/ScenarioDetail.tsx
@src/components/DurationSelector.tsx
@src/components/PromptSelector.tsx
@src/components/AppHeader.tsx
@src/data/techniques.ts
@src/types/technique.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign PreSessionScreen with technique briefing and free practice modes</name>
  <files>src/pages/PreSessionScreen.tsx</files>
  <action>
Redesign PreSessionScreen.tsx to support two distinct modes per user decisions (LOCKED):

**Route handling:**
The screen serves two routes:
- `/practice/:mode/setup` — free practice (filler or pace mode)
- `/practice/technique/setup` — technique practice (receives techniqueId in route state)

Read `techniqueId` from `location.state` to determine which mode to display.

**Mode 1: Technique Practice (when techniqueId present)**

Import technique data via `getTechniqueById(techniqueId)` from `src/data/techniques.ts`.

Layout (top to bottom):
1. **AppHeader** with `showBack={true}`, `onBack` navigates to `/technique/${techniqueId}` (back to technique detail)
2. **Technique Briefing Card** — dark surface card with:
   - Technique name: `text-h3`, `font-display`, `text-text-heading`
   - Framework badge: small pill showing framework name (e.g., "VOSS") with framework color
   - Difficulty badge: small pill with difficulty level
   - "What to practice" section: technique's `practice_prompt` displayed as body text
   - "Success criteria" section: bulleted list from technique's `success_criteria` array
   - Use card styling: `bg-background-surface rounded-2xl p-6 border border-background-elevated`
3. **AI Persona label** — simple text below briefing card:
   - Format: "AI Coach — [style description]" (e.g., "Negotiation Coach — Direct & challenging")
   - Style: `text-body-sm text-text-muted` with small icon prefix
   - Per user decision: name + style label ONLY, no avatar art assets
4. **Duration selector** — existing DurationSelector component, below persona label
5. **Start Practice CTA** — gold button, full width, `btn-primary`, "Start Practice"
   - On click: navigate to `/practice/technique` with state: `{ techniqueId, durationSeconds, practicePrompt, techniqueName }`

**Mode 2: Free Practice (no techniqueId)**

Simplified layout:
1. **AppHeader** with `showBack={true}`, `onBack` navigates to `/` (dashboard)
2. **Title area** — mode title ("Filler Words" or "Speech Pace"), subtitle "Choose your session length"
   - Use `text-h2 font-display text-text-heading` for title
3. **PromptSelector** — existing component for speaking prompts (optional selection)
4. **Duration selector** — existing DurationSelector component
5. **Start Session CTA** — gold button, full width, `btn-primary`, "Start Session"
   - On click: navigate to `/practice/${mode}` with state: `{ durationSeconds, speakingPrompt }`

**Shared styling:**
- Full dark background (`bg-background`)
- Content area max-width `max-w-md` centered
- Generous spacing between sections (`space-y-6`)
- Bottom padding for CTA breathing room

**Update route in App.tsx if needed:**
Add `/practice/technique/setup` route pointing to PreSessionScreen (currently technique practice skips pre-session and goes directly from ScenarioDetail to /practice/technique). The ScenarioDetail "Start Practice" button should navigate to `/practice/technique/setup` instead, passing techniqueId in state. Update the navigation in ScenarioDetail.tsx handleStartPractice to route through the pre-session screen.

Wait — actually, to minimize file touches, handle this within PreSessionScreen itself. The route `/practice/:mode/setup` already exists. For technique mode, we can use `/practice/technique/setup` which matches the `:mode` param. Read techniqueId from location.state (passed from ScenarioDetail). This way ScenarioDetail just needs to change its navigate target from `/practice/technique` to `/practice/technique/setup` with the same state.

Update ScenarioDetail.tsx `handleStartPractice` to navigate to `/practice/technique/setup` instead of `/practice/technique`.
  </action>
  <verify>Build passes: `npm run build`. PreSessionScreen.tsx renders technique briefing card when techniqueId is present. Free practice mode shows simplified view. Both modes have duration selector and CTA button.</verify>
  <done>Pre-session screen shows technique briefing (name, prompt, success criteria, AI persona label) for technique practice and simplified prompt+duration for free practice. Single CTA button starts the session. AppHeader with back arrow is shown.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. Technique mode: briefing card shows technique name, practice prompt, success criteria
3. Technique mode: AI persona shows as text label only (no avatar)
4. Free practice mode: title + prompt selector + duration selector
5. Duration selector appears in both modes
6. Single CTA button at bottom starts the session
7. AppHeader with back arrow is rendered
8. Navigation from ScenarioDetail routes through pre-session screen
</verification>

<success_criteria>
- Technique practice has full briefing card with all relevant info
- Free practice is simplified and quick to start
- Both modes use consistent dark premium styling
- Duration selector works in both modes
- AppHeader integrated with back navigation
</success_criteria>

<output>
After completion, create `.planning/phases/15-new-screens-core-flow/15-03-SUMMARY.md`
</output>
