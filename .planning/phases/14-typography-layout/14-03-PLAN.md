---
phase: 14-typography-layout
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - src/index.css
  - src/components/PracticeSession.tsx
  - src/components/AudioQualityWarning.tsx
  - src/components/SilenceNudge.tsx
  - src/components/BottomControlBar.tsx
  - src/components/SessionOrb.tsx
  - src/components/SessionProgressBar.tsx
  - src/pages/Dashboard.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Recording session is fully immersive — no header, no nav, no status bar visible during active recording; only orb and essential controls"
    - "Dashboard on mobile shows practice cards in a horizontal swipeable carousel (one at a time)"
    - "Audio quality warnings (noise, clipping) display as small muted inline text or icon, not prominent alert banners"
    - "'Take a breath' / silence nudge has soft diffused glow when active, text always readable (not blinding)"
    - "Generous Calm-level spacing throughout: 24px gutters desktop, 16px mobile, spacious vertical rhythm"
    - "All pages use max-width 1200px centered on desktop (except immersive recording which is full-screen)"
    - "Build succeeds with zero TypeScript errors"
  artifacts:
    - path: "src/components/PracticeSession.tsx"
      provides: "Immersive recording mode with hidden chrome"
      contains: "immersive"
    - path: "src/pages/Dashboard.tsx"
      provides: "Mobile carousel for practice cards"
      contains: "CardCarousel"
    - path: "src/index.css"
      provides: "Calm-level spacing utilities and layout tokens"
      contains: "--spacing"
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "src/components/CardCarousel.tsx"
      via: "import CardCarousel for mobile practice card carousel"
      pattern: "CardCarousel"
---

<objective>
Apply generous Calm/Headspace-level spacing, make the recording session fully immersive, add Dashboard mobile carousel, and fix Phase 13 feedback items (warnings too prominent, "Take a breath" button too bright).

Purpose: Addresses Phase 13 feedback items #3, #4, #7, #8, #9 — information density, warning prominence, breathing button glow, and overall layout feeling conducive to learning. Also implements locked decisions: immersive recording, Dashboard mobile carousel, desktop max-width.

Output: Spacious layout across the app, immersive recording experience, mobile Dashboard carousel, subdued warnings, and readable breathing prompts.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-typography-layout/14-CONTEXT.md
@.planning/phases/14-typography-layout/14-01-SUMMARY.md
@.planning/phases/14-typography-layout/14-02-SUMMARY.md
@src/components/PracticeSession.tsx
@src/components/AudioQualityWarning.tsx
@src/components/SilenceNudge.tsx
@src/pages/Dashboard.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spacing tokens + immersive recording + warning/nudge fixes</name>
  <files>src/index.css, src/components/PracticeSession.tsx, src/components/AudioQualityWarning.tsx, src/components/SilenceNudge.tsx, src/components/SessionProgressBar.tsx, src/App.tsx</files>
  <action>
  **1. Add spacing tokens (src/index.css):**

  Add to `:root`:
  ```css
  /* Spacing */
  --spacing-gutter-desktop: 1.5rem;  /* 24px */
  --spacing-gutter-mobile: 1rem;     /* 16px */
  --spacing-section: 2.5rem;         /* 40px vertical between sections */
  --spacing-card-padding: 2rem;      /* 32px internal card padding */
  ```

  Update `.card-surface` and `.card-elevated` to use `p-8` (32px) instead of `p-6` (24px) for more breathing room.

  **2. Immersive recording mode (src/components/PracticeSession.tsx):**

  When `isCapturing` is true (session is active), the recording UI should be fully immersive:

  - The PracticeSession component itself is already full-screen (`min-h-screen`). No changes needed to its outer container.
  - However, the parent route wrappers in App.tsx add a header bar ("Back to Dashboard"). When in a recording session, this header should be hidden.

  Implementation approach:
  - In `src/App.tsx`, the route wrappers (e.g., `FreePracticeFillerRoute`, `FreePracticePaceRoute`) currently render a header bar above PracticeSession. Modify these wrappers to NOT render the header. The PracticeSession component already has its own navigation (BottomControlBar with Stop button) so the header is redundant during recording.
  - Alternatively, if removing the header would break navigation from the pre-recording "tap to start" state, wrap the header in a conditional: show it when `!isCapturing`, hide when `isCapturing`. Since PracticeSession manages isCapturing internally and the wrapper doesn't have access to that state, the cleanest approach is:
    - Remove the sticky header from the route wrappers entirely
    - Add a small "Back" button in PracticeSession's pre-recording state (when `!isCapturing`) — positioned at the top-left, minimal: just "← Back" in muted text with `onClick={() => navigate("/")}`.

  - Hide `SessionProgressBar` border and make it subtle — currently has visible UI chrome. Keep the progress bar but make it a thin colored line at the very top with no container.

  **3. Subdued audio quality warnings (src/components/AudioQualityWarning.tsx):**

  Per user decision: "Warnings (background noise, mic issues): subtle inline — small muted text or icon near relevant content, not interrupting."

  Replace the current prominent alert-style warning with a minimal inline indicator:
  - Remove the colored background (`bg-status-warning/10`), border (`border-status-warning/30`), and alert role
  - Replace with a small, single-line inline display: a small icon (the existing AlertTriangle SVG at 14px) followed by muted text
  - Style: `flex items-center gap-1.5 text-caption text-text-subtle` (very muted, small)
  - Remove the dismiss X button — just show the text subtly, let it disappear when the warning condition clears
  - Keep the `animate-fade-in` for smooth appearance
  - Cap at 1 visible warning (not 2) — show only the most relevant one

  **4. Soft silence nudge glow (src/components/SilenceNudge.tsx):**

  Per user decision: "'Take a breath' button: soft, diffused glow when active — inviting not blinding, text always readable."

  Current issue: The background is too bright when active, making text unreadable.

  Fix:
  - Change the nudge container from `bg-background-surface border border-background-elevated` to a softer approach
  - When visible, use a very subtle outer glow: `shadow-[0_0_30px_rgba(201,168,76,0.08),0_0_60px_rgba(201,168,76,0.04)]`
  - Background: `bg-background-surface/80 backdrop-blur-sm` (slight transparency)
  - Border: `border border-accent/10` (very subtle gold border)
  - Text color: ensure `text-text-body` (muted but readable) — NOT bright white
  - The glow should feel warm and inviting, not like a spotlight

  **5. Global layout max-width:**

  Ensure all main page containers use `max-w-5xl` (1024px) on desktop as default, or `max-w-6xl` (1152px) for wider pages. Currently Dashboard uses `max-w-4xl` (896px) — increase to `max-w-5xl` for better use of desktop space while maintaining premium centered feel.

  Update these files to use `max-w-5xl` where they currently use `max-w-4xl` or `max-w-2xl`:
  - Dashboard.tsx main container
  - App.tsx route wrapper containers

  For the immersive recording mode, PracticeSession should use full width (no max-width constraint).
  </action>
  <verify>
  Run `npm run build` — must pass with zero errors. Verify:
  1. PracticeSession renders without a header bar in all states (pre-recording shows small back button, recording shows only orb + controls)
  2. AudioQualityWarning is a small muted inline indicator, not a prominent banner
  3. SilenceNudge has soft gold glow, readable text
  4. Card padding increased to 32px
  5. Main containers use max-w-5xl
  </verify>
  <done>
  Immersive recording mode active (no header/nav during recording). Warnings subdued to inline muted text. Silence nudge has soft diffused gold glow with readable text. Spacing increased to Calm-level generous (32px card padding, 24px gutters). Desktop max-width set to 1024px centered. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard mobile carousel + section spacing</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
  **1. Mobile practice card carousel:**

  Per user decision: "Dashboard on mobile: horizontal card carousel (swipeable) — matches one-card-at-a-time pattern from results."

  Import `CardCarousel` from `../components/CardCarousel`.

  Replace the current `<div className="grid gap-3">` wrapper around practice cards with a responsive layout:
  - On mobile (< 768px): render practice cards inside `<CardCarousel>` — one card at a time, swipeable
  - On desktop (>= 768px): keep the existing grid layout (cards visible side by side)

  Implementation:
  ```tsx
  {/* Practice Modules */}
  <section>
    <h2 className="text-h5 font-semibold text-text-heading mb-4">Practice</h2>

    {/* Desktop: grid layout */}
    <div className="hidden md:grid md:grid-cols-3 gap-4">
      <PracticeCard ... />
      <PracticeCard ... />
      <PracticeCard ... />
    </div>

    {/* Mobile: carousel */}
    <div className="md:hidden">
      <CardCarousel>
        <PracticeCard ... />
        <PracticeCard ... />
        <PracticeCard ... />
      </CardCarousel>
    </div>
  </section>
  ```

  Note: PracticeCard components are rendered twice in the JSX but only one set is visible at any time (CSS `hidden`/`md:grid`).

  **2. Section spacing:**

  Update the Dashboard `<main>` container spacing:
  - Change `space-y-6` to `space-y-10` (40px between sections — Calm-level generous)
  - Change `py-6` to `py-8` for more top/bottom padding
  - Change `px-4 sm:px-6` to `px-4 md:px-6` for consistent mobile gutters

  **3. Desktop grid for recent sessions (optional refinement):**

  On desktop, if there are 3+ sessions, show session cards in a 2-column grid:
  ```tsx
  <div className="space-y-3 md:grid md:grid-cols-2 md:gap-4 md:space-y-0">
  ```
  This uses the extra desktop width better and reduces vertical scroll.

  **4. Stats section spacing:**

  The "Your Progress" stats section: increase internal spacing from `gap-3 sm:gap-4` to `gap-6` for more breathing room between the three stat columns.

  **Important:** Do NOT change any business logic, data loading, navigation, or component behavior. Only layout/spacing classes change.
  </action>
  <verify>
  Run `npm run build` — must pass with zero errors. Verify:
  1. On mobile viewport (< 768px), practice cards show in a swipeable carousel
  2. On desktop viewport, practice cards show in a 3-column grid
  3. Section spacing is generous (40px between sections)
  4. Stats section has increased breathing room
  5. All navigation and functionality still works
  </verify>
  <done>
  Dashboard mobile uses CardCarousel for practice cards (swipeable, one at a time). Desktop retains grid layout. Section spacing increased to Calm-level generous (40px between sections). Stats spacing improved. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Recording session is immersive — no header/nav visible during active recording
3. Warnings are subtle inline indicators, not prominent banners
4. Silence nudge has soft gold glow, text always readable
5. Dashboard mobile shows practice cards in carousel
6. Desktop layout uses max-w-5xl centered
7. Generous Calm-level spacing throughout (40px sections, 32px card padding)
</verification>

<success_criteria>
- Immersive recording: only orb + controls visible (addresses Phase 13 feedback)
- Subdued warnings: small muted text, not interrupting (addresses feedback #3, #4)
- Soft nudge glow: inviting not blinding (addresses feedback #7)
- Dashboard mobile carousel: one card at a time (addresses feedback #5, #8)
- Calm-level spacing: generous, spacious, meditative feel (addresses feedback #9)
- Max-width 1024px desktop, centered (per requirement LAY-03)
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-typography-layout/14-03-SUMMARY.md`
</output>
