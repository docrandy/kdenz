---
phase: 16-new-screens-advanced
plan: 05
type: execute
wave: 1
depends_on: [16-02, 16-03, 16-04]
files_modified:
  - src/components/AppHeader.tsx
  - src/App.tsx
  - src/pages/PostSessionResults.tsx
  - src/components/SlideTransition.tsx
autonomous: true

must_haves:
  truths:
    - "All Phase 16 screens accessible from navigation (menu items added)"
    - "All routes registered in App.tsx"
    - "SlideTransition includes all new routes with correct depths"
    - "PostSessionResults links to Voice Profile Before/After section"
    - "Practice Bridge accessible from post-session results"
    - "All screens follow dark premium design system (SCR-12)"
    - "Build passes with zero errors"
  artifacts:
    - path: "src/components/AppHeader.tsx"
      provides: "Updated hamburger menu with Voice Profile and Breathing items"
    - path: "src/App.tsx"
      provides: "All Phase 16 routes registered"
    - path: "src/components/SlideTransition.tsx"
      provides: "Route depths updated for all new screens"
  key_links:
    - from: "src/components/AppHeader.tsx"
      to: "src/pages/VoiceProfile.tsx"
      via: "Menu item navigates to /voice-profile"
      pattern: "navigate.*voice-profile"
    - from: "src/components/AppHeader.tsx"
      to: "src/pages/BreathingScreen.tsx"
      via: "Menu item navigates to /breathing"
      pattern: "navigate.*breathing"
---

<objective>
Complete Phase 16 integration by wiring all new screens into navigation, routes, and transitions. Ensure all screens are accessible and properly integrated into the app flow.

Purpose: Complete the v2.0 Dark Premium Transformation by ensuring all Phase 16 screens are fully integrated and accessible throughout the app.

Output: Updated navigation menu, route registrations, transition depths, cross-screen links.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-new-screens-advanced/16-CONTEXT.md
@src/components/AppHeader.tsx
@src/App.tsx
@src/components/SlideTransition.tsx
@src/pages/PostSessionResults.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AppHeader menu with Voice Profile and Breathing</name>
  <files>src/components/AppHeader.tsx</files>
  <action>
Add Voice Profile and Breathing to the hamburger menu in AppHeader.

**Menu order (from top to bottom):**
1. Profile (existing)
2. Voice Profile (new) - 🎙️ icon
3. Breathing (new) - 🧘 icon
4. Library (existing)
5. Settings (existing)
6. Privacy (existing)

**Implementation:**
- Add button for "Voice Profile": `navigate("/voice-profile")`
- Add button for "Breathing": `navigate("/breathing")`
- Use consistent styling with existing menu items
- Icons: emoji or SVG (match existing style)
  </action>
  <verify>Build passes: `npm run build` completes with zero errors. AppHeader menu includes Voice Profile and Breathing items.</verify>
  <done>AppHeader hamburger menu includes Voice Profile and Breathing items with correct navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Register all Phase 16 routes in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Ensure all Phase 16 routes are registered in App.tsx.

**Routes to verify/add:**
- `/practice/analysis` (16-01) - AnalysisLoader
- `/voice-profile` (16-02) - VoiceProfile
- `/practice/bridge` (16-03) - PracticeBridge
- `/breathing` (16-04) - BreathingScreen

**Check:**
- All imports present
- All routes in `<Routes>` block
- Routes placed in logical order (practice routes together, utility pages together)
  </action>
  <verify>Build passes: `npm run build` completes with zero errors. All Phase 16 routes registered in App.tsx.</verify>
  <done>All Phase 16 routes registered in App.tsx with correct imports.</done>
</task>

<task type="auto">
  <name>Task 3: Update SlideTransition with all new route depths</name>
  <files>src/components/SlideTransition.tsx</files>
  <action>
Add all Phase 16 routes to ROUTE_DEPTHS map for correct slide animation direction.

**Routes to add:**
- `/practice/analysis`: depth 3 (same as other practice routes)
- `/voice-profile`: depth 1 (same as profile, settings)
- `/practice/bridge`: depth 3 (same as practice routes)
- `/breathing`: depth 1 (utility page, same as settings)

**Implementation:**
- Add entries to ROUTE_DEPTHS object
- Ensure depths are consistent with navigation flow
- Test that forward/back animations work correctly
  </action>
  <verify>Build passes: `npm run build` completes with zero errors. SlideTransition includes all Phase 16 routes with correct depths.</verify>
  <done>SlideTransition ROUTE_DEPTHS updated with all Phase 16 routes.</done>
</task>

<task type="auto">
  <name>Task 4: Add cross-screen links (PostSessionResults to Voice Profile)</name>
  <files>src/pages/PostSessionResults.tsx</files>
  <action>
Add link from PostSessionResults to Voice Profile Before/After section.

**PostSessionResults.tsx changes:**
- In Analytics tab, add link: "Compare with previous" or "View Progress"
- Link navigates to `/voice-profile#before-after`
- Use hash to scroll to Before/After section (if implemented) or just navigate to page
- Link styling: button or text link, gold accent color

**Optional:**
- Add link in Coaching tab: "See your progress" → `/voice-profile`
- Add link in results summary: "View Profile" → `/voice-profile`
  </action>
  <verify>Build passes: `npm run build` completes with zero errors. PostSessionResults includes link to Voice Profile.</verify>
  <done>PostSessionResults includes link to Voice Profile Before/After section.</done>
</task>

<task type="auto">
  <name>Task 5: Final verification and build check</name>
  <files>All Phase 16 files</files>
  <action>
Run final verification to ensure all Phase 16 screens are integrated correctly.

**Checklist:**
1. All routes accessible (test navigation)
2. All menu items work
3. Build passes with zero errors
4. TypeScript compiles successfully
5. No console errors
6. All screens follow dark premium design system
7. Responsive layouts work on mobile

**Run:**
- `npm run build` - should pass
- `npm run dev` - check for runtime errors
- Manual navigation test (if possible)
  </action>
  <verify>Build passes: `npm run build` completes with zero errors. All Phase 16 screens integrated and accessible.</verify>
  <done>All Phase 16 screens integrated, routes registered, navigation working, build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. All Phase 16 routes registered in App.tsx
3. AppHeader menu includes Voice Profile and Breathing
4. SlideTransition includes all new routes with correct depths
5. PostSessionResults links to Voice Profile
6. All screens accessible from navigation
7. No console errors
8. Dark premium design system consistent across all screens
</verification>

<success_criteria>
- All Phase 16 screens fully integrated into app navigation
- All routes registered and accessible
- Menu items work correctly
- Cross-screen links functional
- Build passes, no TypeScript errors
- Phase 16 complete
</success_criteria>

<output>
After completion, create `.planning/phases/16-new-screens-advanced/16-05-SUMMARY.md`
</output>
