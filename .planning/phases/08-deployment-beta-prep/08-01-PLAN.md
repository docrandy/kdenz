---
phase: 08-deployment-beta-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Production build completes without errors"
    - "App deploys successfully to Vercel production"
    - "Deployed app is accessible via public URL"
  artifacts:
    - path: "dist/"
      provides: "Production build output"
  key_links:
    - from: "npm run build"
      to: "dist/index.html"
      via: "Vite build process"
      pattern: "build completed"
---

<objective>
Verify production build and deploy to Vercel production.

Purpose: Ensure the app builds cleanly and is deployed to a public URL for beta testers.
Output: Production Vercel deployment with public URL confirmed.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vercel.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify production build</name>
  <files>dist/</files>
  <action>
    Run `npm run build` to create production build.
    Verify:
    - Build completes without errors or warnings
    - dist/ folder is created with index.html and assets
    - No TypeScript errors
    - No ESLint errors blocking build

    If build fails, diagnose and fix issues before proceeding.
  </action>
  <verify>`npm run build` exits with code 0, dist/index.html exists</verify>
  <done>Production build completes successfully</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel production</name>
  <files></files>
  <action>
    Run `vercel --prod` to deploy to production.

    Note: The project is already linked to Vercel from Phase 01 deployment.
    If not authenticated, the Vercel CLI will prompt for login.

    Capture the production URL from the deployment output.
    Verify the deployment succeeds and the URL is accessible.
  </action>
  <verify>`vercel --prod` succeeds, production URL returned, URL loads in browser</verify>
  <done>App deployed to Vercel production with public URL</done>
</task>

<task type="auto">
  <name>Task 3: Run copy-lint compliance check</name>
  <files></files>
  <action>
    Run `npm run copy-lint` to verify no language boundary violations exist before distribution.

    This ensures all UI strings comply with locked language boundaries (no judgment words).
  </action>
  <verify>`npm run copy-lint` exits with code 0, no violations reported</verify>
  <done>Copy-lint passes with 0 violations</done>
</task>

</tasks>

<verification>
- Production build exits cleanly (exit code 0)
- Vercel deployment succeeds
- Production URL is accessible
- Copy-lint compliance verified
</verification>

<success_criteria>
- Build completes without errors
- App deployed to Vercel production
- Production URL confirmed and accessible
- Language boundary compliance verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-deployment-beta-prep/08-01-SUMMARY.md`
</output>
