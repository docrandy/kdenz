---
phase: 05-transcript-highlights
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/EvaluationPage.tsx
  - src/App.tsx
  - src/pages/PostSessionResults.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate from PostSessionResults to Evaluation page"
    - "Evaluation page displays transcript with toggle-able highlighting"
    - "Only one highlight mode visible at a time (filler OR pace)"
  artifacts:
    - path: "src/pages/EvaluationPage.tsx"
      provides: "Full evaluation page with transcript and highlight toggle"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "Route for /practice/evaluation"
      contains: "EvaluationPage"
  key_links:
    - from: "src/pages/PostSessionResults.tsx"
      to: "/practice/evaluation"
      via: "useNavigate"
      pattern: "navigate.*evaluation"
    - from: "src/pages/EvaluationPage.tsx"
      to: "src/components/TranscriptView.tsx"
      via: "import and render"
      pattern: "<TranscriptView"
---

<objective>
Create Evaluation page with transcript display and highlight toggle for deep session review

Purpose: Provide a dedicated "deep dive" destination where users can study their transcript with filler or pace highlights
Output: EvaluationPage component with TranscriptView and HighlightToggle, accessible via PostSessionResults
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-transcript-highlights/05-01-SUMMARY.md
@src/components/TranscriptView.tsx
@src/components/HighlightToggle.tsx
@src/pages/PostSessionResults.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EvaluationPage component</name>
  <files>src/pages/EvaluationPage.tsx</files>
  <action>
    Create new file `src/pages/EvaluationPage.tsx`:

    ```typescript
    import { useEffect, useState } from 'react';
    import { useNavigate } from 'react-router-dom';
    import TranscriptView from '../components/TranscriptView';
    import HighlightToggle, { HighlightMode } from '../components/HighlightToggle';
    import { WordTiming } from '../core/audio/useWebSpeech';
    import { ReconciledFiller } from '../lib/fillerReconciler';

    interface SessionEvalData {
      transcript: string;
      wordTimings: WordTiming[];
      reconciledFillers: ReconciledFiller[];
      durationSeconds: number;
      fillerCount: number;
      wpm: number;
      focusMode: 'filler' | 'pace';
    }

    export default function EvaluationPage() {
      const navigate = useNavigate();
      const [sessionData, setSessionData] = useState<SessionEvalData | null>(null);
      const [highlightMode, setHighlightMode] = useState<HighlightMode>('fillers');

      useEffect(() => {
        try {
          const stored = sessionStorage.getItem('voicelab_last_session');
          if (!stored) {
            navigate('/');
            return;
          }
          const data = JSON.parse(stored);
          // Validate required fields for evaluation
          if (!data.transcript || !data.wordTimings) {
            navigate('/practice/results');
            return;
          }
          setSessionData({
            transcript: data.transcript,
            wordTimings: data.wordTimings || [],
            reconciledFillers: data.reconciledFillers || [],
            durationSeconds: data.durationSeconds,
            fillerCount: data.fillerCount,
            wpm: data.wpm,
            focusMode: data.focusMode,
          });
          // Default to filler highlight for filler mode, pace for pace mode
          setHighlightMode(data.focusMode === 'filler' ? 'fillers' : 'pace');
        } catch {
          navigate('/');
        }
      }, [navigate]);

      if (!sessionData) {
        return null;
      }

      const handleBack = () => {
        navigate('/practice/results');
      };

      return (
        <div className="min-h-screen bg-white">
          {/* Header */}
          <div className="bg-white border-b sticky top-0 z-10">
            <div className="max-w-2xl mx-auto px-4 py-4 flex items-center justify-between">
              <button
                onClick={handleBack}
                className="text-gray-600 hover:text-gray-900 flex items-center gap-2"
              >
                ← Back to Results
              </button>
              <h1 className="text-lg font-semibold text-gray-900">Session Transcript</h1>
              <div className="w-24" /> {/* Spacer for centering */}
            </div>
          </div>

          {/* Main content */}
          <div className="max-w-2xl mx-auto px-4 py-6 space-y-6">
            {/* Highlight toggle */}
            <div className="flex justify-center">
              <HighlightToggle
                mode={highlightMode}
                onChange={setHighlightMode}
                fillerCount={sessionData.reconciledFillers.length}
              />
            </div>

            {/* Transcript with highlighting */}
            <div className="bg-white border rounded-lg shadow-sm">
              <TranscriptView
                transcript={sessionData.transcript}
                reconciledFillers={sessionData.reconciledFillers}
                wordTimings={sessionData.wordTimings}
                highlightMode={highlightMode}
                sessionDurationMs={sessionData.durationSeconds * 1000}
              />
            </div>

            {/* Session stats summary */}
            <div className="flex justify-center gap-8 text-sm text-gray-500">
              <span>{sessionData.fillerCount} filler{sessionData.fillerCount !== 1 ? 's' : ''}</span>
              <span>{sessionData.wpm} WPM</span>
              <span>{Math.floor(sessionData.durationSeconds / 60)}m {sessionData.durationSeconds % 60}s</span>
            </div>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>
    File exists at src/pages/EvaluationPage.tsx with correct imports and component structure
  </verify>
  <done>
    EvaluationPage component created with TranscriptView and HighlightToggle
  </done>
</task>

<task type="auto">
  <name>Task 2: Add route for EvaluationPage</name>
  <files>src/App.tsx</files>
  <action>
    1. Add import near other page imports:
       `import EvaluationPage from './pages/EvaluationPage'`

    2. Add route inside <Routes> after the /practice/results route:
       `<Route path="/practice/evaluation" element={<EvaluationPage />} />`

    Place it after the existing practice routes for logical grouping.
  </action>
  <verify>
    Run `npm run build` — no TypeScript errors related to EvaluationPage route
  </verify>
  <done>
    Route /practice/evaluation added to App.tsx
  </done>
</task>

<task type="auto">
  <name>Task 3: Add "View Transcript" button to PostSessionResults</name>
  <files>src/pages/PostSessionResults.tsx</files>
  <action>
    1. In the navigation bar section (the grid with Dashboard/Try Again/New Session), add a fourth button:
       Change `grid-cols-3` to `grid-cols-2 sm:grid-cols-4`

    2. Add the View Transcript button:
       ```tsx
       <button
         onClick={() => navigate('/practice/evaluation')}
         className="px-4 py-3 bg-white border-2 border-gray-300 rounded-lg text-gray-700 font-medium hover:bg-gray-50 transition-colors"
       >
         View Transcript
       </button>
       ```

    3. Alternatively, if the grid becomes too crowded, add a secondary link above the navigation bar:
       ```tsx
       {/* View Transcript link */}
       {sessionData.transcript && sessionData.wordTimings && (
         <div className="text-center">
           <button
             onClick={() => navigate('/practice/evaluation')}
             className="text-clinical-accent hover:underline text-sm"
           >
             View full transcript with highlights →
           </button>
         </div>
       )}
       ```

    Recommended: Use the secondary link approach above the navigation bar for cleaner UI.
  </action>
  <verify>
    Run `npm run build` — no errors
    Navigate to /practice/results after a session and verify "View Transcript" link is visible
  </verify>
  <done>
    PostSessionResults has link to navigate to /practice/evaluation
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Complete a practice session
3. On PostSessionResults page, click "View full transcript with highlights"
4. EvaluationPage loads with:
   - Back button to Results
   - Highlight toggle (None/Fillers/Pace)
   - Transcript text with highlighting based on selected mode
   - Session stats at bottom
5. Toggle between modes — only one highlight type visible at a time
</verification>

<success_criteria>
- EvaluationPage component exists and renders correctly
- Route /practice/evaluation is accessible
- PostSessionResults has link to evaluation page
- Highlight toggle works (only one mode visible at a time)
</success_criteria>

<output>
After completion, create `.planning/phases/05-transcript-highlights/05-02-SUMMARY.md`
</output>
