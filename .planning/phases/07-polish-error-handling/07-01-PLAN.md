---
phase: 07-polish-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/audio/useAudioCapture.ts
  - src/components/AudioQualityWarning.tsx
  - src/components/PracticeSession.tsx
  - src/pages/PostSessionResults.tsx
autonomous: true

must_haves:
  truths:
    - "User sees warning when background noise is detected"
    - "User sees warning when microphone clipping is detected"
    - "Warning copy matches foundation docs verbatim"
  artifacts:
    - path: "src/components/AudioQualityWarning.tsx"
      provides: "Audio quality warning display component"
      min_lines: 30
  key_links:
    - from: "src/components/PracticeSession.tsx"
      to: "AudioQualityWarning"
      via: "conditional render during session"
      pattern: "<AudioQualityWarning"
---

<objective>
Add audio quality monitoring with real-time warnings for noise and clipping.

Purpose: Users need to know when audio quality issues may affect metric reliability (research-validated UX).
Output: AudioQualityWarning component, audio quality detection in useAudioCapture hook, warnings displayed during session.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/foundation/copy/audio-quality-warning-messages-v1.md
@src/core/audio/useAudioCapture.ts
@src/components/PracticeSession.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audio quality detection to useAudioCapture hook</name>
  <files>src/core/audio/useAudioCapture.ts</files>
  <action>
Add real-time audio quality monitoring to the useAudioCapture hook:

1. Add quality state:
   - `noiseLevel: number` (0-1, computed from frequency analysis)
   - `isClipping: boolean` (true if samples exceed threshold)
   - `qualityWarnings: ('noise' | 'clipping')[]`

2. Implement noise detection:
   - Create AnalyserNode if not already present
   - Compute noise floor from low frequencies (< 300Hz)
   - Flag as noisy if noise floor exceeds 0.3 threshold
   - Use frequency bin analysis, not just RMS

3. Implement clipping detection:
   - Check if any time-domain samples reach -1.0 or 1.0
   - Flag as clipping if 3+ consecutive samples at max
   - Use getByteTimeDomainData from existing analyser

4. Expose in hook return:
   - Add `qualityWarnings` to UseAudioCaptureResult
   - Update on each animation frame with debounce (check every 500ms)

Do NOT use judgment language in detection logic (no "bad", "poor", etc.).
  </action>
  <verify>Hook compiles without errors, qualityWarnings array is exposed</verify>
  <done>useAudioCapture hook exposes qualityWarnings: ('noise' | 'clipping')[] that updates during capture</done>
</task>

<task type="auto">
  <name>Task 2: Create AudioQualityWarning component</name>
  <files>src/components/AudioQualityWarning.tsx</files>
  <action>
Create a new component that displays audio quality warnings:

1. Props:
   - `warnings: ('noise' | 'clipping')[]`
   - `className?: string`

2. Warning messages (VERBATIM from foundation docs):
   - noise: "Background noise detected; pause metrics may be inaccurate."
   - clipping: "Microphone clipping detected; pitch/volume estimates may be distorted."

3. Visual design:
   - Yellow/amber warning style (clinical-warm-amber from design system)
   - Small banner at top of session area
   - Warning icon (triangle with exclamation)
   - Dismissible with X button (per warning)
   - Max 2 warnings visible at once (stack vertically)

4. Behavior:
   - Show only when warnings array is non-empty
   - Fade in/out animation (transition-opacity)
   - Once dismissed, don't re-show same warning for current session

Use clinical-warm-amber for warning color per design system.
  </action>
  <verify>Component renders with correct copy, matches design system colors</verify>
  <done>AudioQualityWarning component displays warnings with verbatim foundation copy</done>
</task>

<task type="auto">
  <name>Task 3: Wire warnings into PracticeSession</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Integrate AudioQualityWarning into the practice session flow:

1. Import AudioQualityWarning component

2. Destructure qualityWarnings from useAudioCapture hook

3. Add warning display:
   - Position below SessionProgressBar, above SessionOrb
   - Only show when isCapturing && !isPaused
   - Pass qualityWarnings array to component

4. Track dismissed warnings:
   - Add `dismissedWarnings` state (Set<'noise' | 'clipping'>)
   - Filter out dismissed warnings before passing to component
   - Reset dismissedWarnings on handleStart

5. Export qualityWarnings in session data:
   - Add to sessionStorage save in handleStop
   - Include which warnings were shown during session

Layout: SessionProgressBar -> AudioQualityWarning -> SessionOrb -> focus feedback
  </action>
  <verify>npm run build succeeds, warnings display during session when quality issues detected</verify>
  <done>Audio quality warnings appear during session when noise or clipping is detected</done>
</task>

</tasks>

<verification>
- [ ] npm run build completes without errors
- [ ] npm run lint passes
- [ ] AudioQualityWarning component exists and exports correctly
- [ ] useAudioCapture exposes qualityWarnings
- [ ] Warning copy matches foundation docs exactly
</verification>

<success_criteria>
- Audio quality warnings display during session when issues detected
- Warning messages use verbatim foundation copy
- Warnings are dismissible and don't re-appear after dismissal
- Design uses clinical-warm-amber per design system
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-error-handling/07-01-SUMMARY.md`
</output>
