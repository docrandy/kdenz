---
phase: 07-polish-error-handling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/copy-lint.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Copy-lint script detects banned words from language-boundaries-v1.md"
    - "Script scans all TSX/TS files in src/"
    - "Script reports violations with file and line number"
  artifacts:
    - path: "scripts/copy-lint.ts"
      provides: "Copy linting script for language boundary enforcement"
      min_lines: 50
  key_links:
    - from: "package.json"
      to: "scripts/copy-lint.ts"
      via: "npm script"
      pattern: "\"copy-lint\""
---

<objective>
Create copy-lint script to enforce language boundaries from foundation docs.

Purpose: Ensure all UI strings comply with locked language boundaries. Hard-banned words must never appear in product.
Output: Executable script that scans codebase and reports violations, npm script for easy execution.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/foundation/product/language-boundaries-v1.md
@docs/foundation/copy/claim-taxonomy-safe-risky-forbidden-v1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create copy-lint script</name>
  <files>scripts/copy-lint.ts</files>
  <action>
Create a TypeScript script that enforces language boundaries:

1. Define banned words array (from language-boundaries-v1.md):
   - Diagnostic/Clinical: "diagnose", "diagnosis", "disorder", "therapy", "therapeutic", "patient", "treatment", "symptom"
   - Psychological: "anxious", "anxiety", "confident", "confidence score", "nervous", "stressed", "emotional state"
   - Judgment: "good" (when about speech), "bad" (when about speech), "correct", "incorrect", "improve" (imperative), "you need to", "weakness", "problem" (about speech), "normal", "abnormal"
   - Accuracy overreach: "accurately detects", "proven to", "scientifically validated", "AI-powered insights"

2. Context-aware detection:
   - Scan string literals in TSX/TS files
   - Match whole words only (word boundaries)
   - Ignore code comments (allow technical discussion)
   - Check JSX text content and string props

3. Output format:
   - File path and line number
   - The banned word found
   - Surrounding context (10 chars before/after)
   - Exit code 1 if violations found, 0 if clean

4. CLI execution:
   - Use ts-node or tsx for execution
   - Accept optional path argument (default: src/)
   - Support --fix flag (placeholder for future auto-fix)

Script should use Node.js fs and path modules. Parse files with simple regex (no AST needed for MVP).
  </action>
  <verify>Script compiles, can be run with npx tsx scripts/copy-lint.ts</verify>
  <done>Copy-lint script exists and can scan files for banned words</done>
</task>

<task type="auto">
  <name>Task 2: Add npm script and run initial scan</name>
  <files>package.json</files>
  <action>
1. Add npm script to package.json:
   ```json
   "scripts": {
     "copy-lint": "tsx scripts/copy-lint.ts src/"
   }
   ```

2. Ensure tsx is available (should already be installed for Vite)

3. Run initial scan:
   - Execute `npm run copy-lint`
   - Document any violations found
   - Create list of files that need attention

4. If violations found:
   - Do NOT auto-fix
   - List violations in summary for Phase 08 or manual fix
   - Script should succeed if it runs without error (even with violations)

Note: Actual violations should be logged but not block this task.
The script's job is detection, not enforcement (that's manual review).
  </action>
  <verify>npm run copy-lint executes and reports results</verify>
  <done>npm run copy-lint script works and reports any language boundary violations</done>
</task>

</tasks>

<verification>
- [ ] scripts/copy-lint.ts exists and is valid TypeScript
- [ ] npm run copy-lint executes without runtime errors
- [ ] Script outputs violations with file/line info
- [ ] Banned word list matches language-boundaries-v1.md
</verification>

<success_criteria>
- Copy-lint script detects banned words from foundation docs
- Script can be run via npm run copy-lint
- Violations reported with actionable file/line information
- Script scans all TSX/TS files in src/
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-error-handling/07-03-SUMMARY.md`
</output>
