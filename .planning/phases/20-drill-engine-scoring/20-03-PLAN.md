---
phase: 20-drill-engine-scoring
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - src/features/drill-engine/DrillScreen.tsx
  - src/features/drill-engine/FeedbackCard.tsx
  - src/features/drill-engine/index.ts
autonomous: true

must_haves:
  truths:
    - "User can read a scenario prompt and tap Record to start speaking"
    - "Live transcript appears while recording (text updates in real time)"
    - "Tapping Stop submits transcript — form score appears immediately, Gemini scores appear after spinner"
    - "FeedbackCard shows Form/Accuracy/Impact as percentages with label text (Mastered/Strong/Close/Needs work)"
    - "Next Scenario button loads a fresh scenario without leaving the drill context"
    - "Attempt count (Attempt 3) and streak badge (Streak: 2) are visible during the session"
    - "Non-Chrome browser shows inline warning banner (not a blocking modal)"
    - "Transcription error message is shown inline with a Retry button"
    - "On Gemini failure, FeedbackCard shows form score and 'Accuracy/Impact pending' message"
  artifacts:
    - path: "src/features/drill-engine/DrillScreen.tsx"
      provides: "Main drill UI — scenario display, recording controls, session counters"
      exports: ["DrillScreen"]
    - path: "src/features/drill-engine/FeedbackCard.tsx"
      provides: "Score breakdown card with coaching narrative"
      exports: ["FeedbackCard"]
    - path: "src/features/drill-engine/index.ts"
      provides: "Barrel export for the drill-engine feature"
      exports: ["DrillScreen"]
  key_links:
    - from: "src/features/drill-engine/DrillScreen.tsx"
      to: "src/features/drill-engine/useDrillSession.ts"
      via: "useDrillSession(techniqueId) hook call"
      pattern: "useDrillSession"
    - from: "src/features/drill-engine/DrillScreen.tsx"
      to: "Web Speech API"
      via: "window.SpeechRecognition / webkitSpeechRecognition"
      pattern: "SpeechRecognition|webkitSpeechRecognition"
    - from: "src/features/drill-engine/DrillScreen.tsx"
      to: "src/features/drill-engine/FeedbackCard.tsx"
      via: "renders FeedbackCard when state === 'feedback'"
      pattern: "FeedbackCard"
---

<objective>
Build the drill UI — DrillScreen, FeedbackCard, and the feature barrel export.

Purpose: The complete user-facing experience for a drill session. DrillScreen owns the
Web Speech API interaction (Chrome detection, mic, live transcript) and delegates all
business logic to useDrillSession. FeedbackCard renders the score breakdown.

Output:
- src/features/drill-engine/DrillScreen.tsx
- src/features/drill-engine/FeedbackCard.tsx
- src/features/drill-engine/index.ts
</objective>

<execution_context>
@C:\Users\randy\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\randy\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@src/features/drill-engine/useDrillSession.ts
@src/features/drill-engine/drillEvaluationService.ts
@src/features/labeling/LabelingPractice.tsx
@src/components/BrowserWarning.tsx
@src/utils/browserDetection.ts

Key patterns:
- Recording: see LabelingPractice.tsx (handleStartRecording/stopRecording pattern — reuse this verbatim)
- Chrome detection: isChrome() from src/utils/browserDetection.ts (already used in App.tsx)
- Design system: dark premium tokens — btn-primary (gold), card-surface, text-text-heading, text-accent
- Waveform: for Phase 20 use a simple animated bar visualization (no external lib needed — CSS animation)
- Record button: gold background (btn-primary), full-width, prominent
- Stop button: red background (bg-status-error text-text-inverse), same size as record button
- Layout: single-column, max-w-2xl mx-auto px-4 py-6
- Typography: technique name uses font-display (Cormorant), body uses default (Outfit)
- Score labels: 90-100 = Mastered, 75-89 = Strong/Well-formed/Effective, 60-74 = Close/Adequate/Moderate, <60 = Needs work/Off-target/Weak
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/features/drill-engine/FeedbackCard.tsx — score breakdown card</name>
  <files>src/features/drill-engine/FeedbackCard.tsx</files>
  <action>
    Create FeedbackCard as a pure display component. No hooks, no state, no logic.

    Props interface:
    interface FeedbackCardProps {
      techniqueName: string;
      formScore: number;
      accuracyScore: number | null;
      impactScore: number | null;
      compositeScore: number;
      explanation: string;
      geminiSucceeded: boolean;
      geminiLoading: boolean;       // true when Gemini call is in flight
      onNext: () => void;           // "Next Scenario" button handler
      attemptCount: number;
      streak: number;
    }

    LAYOUT (match the card structure from CONTEXT.md):
    ```
    [Technique Name / Title]  ← font-display, text-h4, text-text-heading
    ━━━━━━━━━━━━━━━━━━━━━━   ← border-t border-background-elevated
    Form: 85% — Well-formed  ← score row
    Accuracy: 72% — Close    ← score row (or spinner if geminiLoading)
    Impact: 78% — Effective  ← score row (or spinner if geminiLoading)
    ━━━━━━━━━━━━━━━━━━━━━━
    Composite: 78% — Strong  ← composite row, slightly de-emphasized
    ━━━━━━━━━━━━━━━━━━━━━━
    [Coaching text]           ← explanation, 3-4 lines, text-body text-text-body
    ━━━━━━━━━━━━━━━━━━━━━━
    [Next Scenario Button]    ← btn-primary w-full
    ```

    SCORE LABEL FUNCTION (internal helper):
    function scoreLabel(score: number, dimension: 'form' | 'accuracy' | 'impact' | 'composite'): string
      Labels by dimension:
      - form:      90-100="Mastered"  75-89="Well-formed"  60-74="Close"        <60="Needs work"
      - accuracy:  90-100="Mastered"  75-89="Strong"       60-74="Adequate"     <60="Off-target"
      - impact:    90-100="Mastered"  75-89="Effective"    60-74="Moderate"     <60="Weak"
      - composite: 90-100="Mastered"  75-89="Strong"       60-74="Close"        <60="Needs work"

    SCORE COLOR FUNCTION (internal helper):
    function scoreColor(score: number): string
      90-100: text-status-success
      75-89:  text-accent  (gold)
      60-74:  text-text-muted
      <60:    text-status-error

    SCORE ROW component:
    Show: "[Dimension]: [Score]% — [Label]"
    When geminiLoading AND (dimension is accuracy OR impact):
      Show: "[Dimension]: " + small spinning indicator + "Scoring..."
    When geminiSucceeded === false AND (dimension is accuracy OR impact) AND not geminiLoading:
      Show: "[Dimension]: Pending" in text-text-subtle

    ATTEMPT/STREAK BADGES:
    Small row above the divider: "Attempt {attemptCount}" | "Streak: {streak}"
    Only show streak if streak > 0
    Style: text-caption text-text-muted, flex gap-4

    COMPOSITE SCORE:
    Slightly smaller than main scores, de-emphasized — it is the summary number.
    Show only when both accuracyScore and impactScore are available (not loading/null).
    When Gemini failed: show "Score: {formScore}% (form only)" in text-text-subtle.

    GRACEFUL DEGRADATION — Gemini failed:
    - Accuracy row: "Accuracy: — (Pending)" in text-text-subtle
    - Impact row: "Impact: — (Pending)" in text-text-subtle
    - Composite: "{formScore}% (form only)"
    - Explanation text: show the fallback explanation string from evaluateDrillResponse

    WRAPPER STYLES:
    div.card-surface.space-y-4 (uses existing CSS class from design system)
    Inner sections: separated by border-t border-background-elevated
  </action>
  <verify>
    Run: npm run type-check
    Expected: 0 TypeScript errors in FeedbackCard.tsx
    Check: geminiLoading=true → accuracy/impact rows show spinner text
    Check: geminiSucceeded=false, geminiLoading=false → Pending shown for accuracy/impact
    Check: all score labels correct for boundary values (75, 89, 90, 60, 74, 59)
  </verify>
  <done>
    - src/features/drill-engine/FeedbackCard.tsx exports FeedbackCard
    - Score rows display correctly for all three states: loaded, loading, failed
    - Score label function handles all 4 dimensions with correct tier labels
    - "Next Scenario" button calls onNext prop
    - Attempt count and streak badges rendered
    - npm run type-check passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/features/drill-engine/DrillScreen.tsx — main drill UI with recording</name>
  <files>src/features/drill-engine/DrillScreen.tsx</files>
  <action>
    Create DrillScreen — the main container for a drill session. This is the component
    App.tsx will route to. It owns Web Speech API setup and delegates all logic to useDrillSession.

    PROPS:
    interface DrillScreenProps {
      techniqueId: string;   // e.g., 'mirroring', 'labeling'
      onBack: () => void;    // navigate back to /skills-lab
    }

    CHROME DETECTION (inline, non-blocking):
    - Import isChrome from src/utils/browserDetection.ts
    - If !isChrome(): show inline banner at top of screen (NOT blocking modal):
      ```
      <div className="bg-status-warning/10 border-b border-status-warning/30 px-4 py-3 text-center text-body-sm text-status-warning">
        Voice drills require Chrome for accurate transcription. You may see errors in {getBrowserName()}.
      </div>
      ```
    - Continue rendering drill below the banner regardless

    WEB SPEECH API SETUP (mirror LabelingPractice.tsx pattern exactly):
    - useRef for recognitionRef, useRef for isRecordingRef (avoid stale closure)
    - useEffect on mount: create SpeechRecognition, set continuous=true, interimResults=true
    - onresult handler: collect final transcripts, call hook.appendTranscript(finalText)
    - onerror handler: console.error, call stopRecording()
    - cleanup: recognitionRef.current?.abort() on unmount

    RECORDING WAVEFORM (simple animated visualization — no external lib):
    Show while state === 'recording': 5 vertical bars with CSS animation:
    ```jsx
    <div className="flex items-center justify-center gap-1 h-8">
      {[0,1,2,3,4].map(i => (
        <div key={i}
          className="w-1.5 rounded-full bg-accent"
          style={{
            height: '100%',
            animation: `drillWave 0.8s ease-in-out ${i * 0.1}s infinite alternate`,
            transform: 'scaleY(0.3)'
          }}
        />
      ))}
    </div>
    ```
    Add the keyframe animation to the component file via a style tag or inline object:
    Use a <style> tag inside the component return with the @keyframes drillWave rule:
    @keyframes drillWave { from { transform: scaleY(0.3); } to { transform: scaleY(1.0); } }

    LAYOUT — single column, full state machine render:

    STATE: idle (before startSession called)
    - Show nothing; useEffect calls hook.startSession() on mount automatically

    STATE: presenting
    - Technique name: className="text-h4 font-display font-semibold text-text-heading"
    - Syntax template reminder: small badge below name
      "Template: {technique.syntax_template}" in text-caption text-text-muted
    - Scenario prompt box: card-surface, prominent, text-body text-text-body
    - Inline error (if hook.error): red banner with "Try Again" button (calls clearError, stays in presenting)
    - Record button: btn-primary w-full large
      Label: "Tap to Record Response"
      onClick: calls handleStartRecording() (defined below)
    - Attempt/Streak counters (if attemptCount > 0):
      "Attempt {attemptCount}" | "Streak: {streak}" in text-caption text-text-muted

    STATE: recording
    - Technique name (same as presenting)
    - Scenario prompt (same, dimmed slightly — opacity-60)
    - Waveform animation (5 bars)
    - Live transcript box (shows if hook.transcript is non-empty):
      "Hearing: "{hook.transcript}"" — italic, bg-background-elevated rounded p-3
    - Stop button: bg-status-error text-text-inverse w-full large
      Label: "Stop & Submit"
      onClick: calls handleStop()

    STATE: scoring
    - Show: centered spinner (existing animate-spin pattern)
      text below: "Scoring your response..."
      Sub-text: "Form score ready. Analyzing accuracy and impact..."

    STATE: feedback
    - Render FeedbackCard with all props from hook.evaluation + geminiLoading (pass false here since scoring is done)
      NOTE: geminiLoading is true while in 'scoring' state, false in 'feedback' state
      Pass geminiLoading={false} to FeedbackCard when state='feedback' (Gemini already done)
    - FeedbackCard.onNext = () => hook.nextScenario()

    STATE: complete
    - "No more scenarios. Excellent practice!"
    - "Back to Skills Lab" button → onBack()

    RECORDING HANDLERS (mirror LabelingPractice.tsx pattern):
    handleStartRecording = async () => {
      hook.startRecording()                    // transition state
      recognitionRef.current?.start()          // start speech recognition
    }

    handleStop = () => {
      recognitionRef.current?.stop()
      setTimeout(() => hook.submitTranscript(), 500)  // 500ms delay for final transcript
    }

    HEADER:
    Sticky header (same pattern as LabelingPractice.tsx):
    - Back button: "← Back" → onBack()
    - Title: "Skill Drill" (generic — technique name shown in body)
    - No summary button needed in Phase 20

    INITIALIZATION:
    useEffect(() => { hook.startSession() }, [])
    (call startSession once on mount; techniqueId is stable prop)

    IMPORTS:
    - { useState, useEffect, useRef } from 'react'
    - { isChrome, getBrowserName } from '../../utils/browserDetection'
    - { useDrillSession } from './useDrillSession'
    - { FeedbackCard } from './FeedbackCard'
  </action>
  <verify>
    Run: npm run type-check
    Expected: 0 TypeScript errors in DrillScreen.tsx

    Check: component accepts techniqueId and onBack props
    Check: Web Speech API refs created correctly (recognitionRef is useRef<any>(null))
    Check: handleStop calls recognitionRef.current?.stop() before submitTranscript
    Check: all 5 states render without crash (idle/presenting/recording/scoring/feedback/complete)
  </verify>
  <done>
    - src/features/drill-engine/DrillScreen.tsx exports DrillScreen
    - All 6 DrillState variants render without crash
    - Web Speech API initialized in useEffect with cleanup on unmount
    - Chrome banner shown inline when !isChrome() (non-blocking)
    - Recording → waveform animation renders
    - Live transcript updates as recognitionRef fires
    - handleStop delays 500ms before submitTranscript (matches LabelingPractice pattern)
    - FeedbackCard receives all props including geminiLoading=false in feedback state
    - npm run type-check passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create src/features/drill-engine/index.ts — barrel export</name>
  <files>src/features/drill-engine/index.ts</files>
  <action>
    Create the barrel export file for the drill-engine feature module.

    Content:
    export { DrillScreen } from './DrillScreen';

    That is the only export needed for Phase 20. App.tsx imports DrillScreen from this barrel.
    (useDrillSession is internal to the feature — not exported from barrel in Phase 20.)
  </action>
  <verify>
    File exists. Content is single export line. npm run type-check passes.
  </verify>
  <done>
    - src/features/drill-engine/index.ts exists
    - Exports DrillScreen from ./DrillScreen
    - npm run type-check passes
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. npm run type-check — 0 errors
2. npm run build — 0 errors, 0 new warnings
3. Three files exist: DrillScreen.tsx, FeedbackCard.tsx, index.ts
4. FeedbackCard handles all three score states (loaded / loading / failed)
5. DrillScreen renders all 6 DrillState variants
6. Chrome inline banner appears for non-Chrome (non-blocking)
7. Recording uses useRef pattern matching LabelingPractice.tsx
</verification>

<success_criteria>
- FeedbackCard displays Form/Accuracy/Impact with correct label tiers (Mastered/Strong/Close/Needs work)
- FeedbackCard shows spinner text when geminiLoading=true
- FeedbackCard shows "Pending" for accuracy/impact when geminiSucceeded=false
- DrillScreen wires useDrillSession hook correctly — no business logic in component
- Web Speech API integration follows existing LabelingPractice.tsx pattern exactly
- 500ms delay on stop before submitTranscript (final transcript collection)
- npm run type-check and npm run build pass clean
</success_criteria>

<output>
After completion, create .planning/phases/20-drill-engine-scoring/20-03-SUMMARY.md with:
- What was built
- Component props interfaces
- Any deviations from plan
- Status: COMPLETE
</output>
