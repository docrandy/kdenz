---
phase: 20-drill-engine-scoring
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - src/App.tsx
  - src/pages/SkillsPage.tsx
autonomous: false

must_haves:
  truths:
    - "Navigating to /practice/drill/:techniqueId renders DrillScreen for that technique"
    - "SkillsPage shows 8 drill cards — one per Tier A technique — each navigating to its drill route"
    - "Tapping Back inside DrillScreen returns to /skills-lab"
    - "Non-drill routes (labeling, accusation-audit, voice-lab, etc.) continue to work unchanged"
    - "All 8 technique drills accessible from SkillsPage in one tap"
  artifacts:
    - path: "src/App.tsx"
      provides: "Route for /practice/drill/:techniqueId → DrillScreen"
      exports: ["default App"]
    - path: "src/pages/SkillsPage.tsx"
      provides: "Updated Skills Lab hub with 8 drill cards"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/drill-engine/index.ts"
      via: "import DrillScreen"
      pattern: "DrillScreen.*drill-engine"
    - from: "src/App.tsx"
      to: "Route /practice/drill/:techniqueId"
      via: "useParams to extract techniqueId"
      pattern: "useParams"
    - from: "src/pages/SkillsPage.tsx"
      to: "navigate('/practice/drill/:techniqueId')"
      via: "useNavigate with techniqueId"
      pattern: "navigate.*drill"
---

<objective>
Wire DrillScreen into the app routing and update SkillsPage with 8 drill cards.

Purpose: DrillScreen exists but is unreachable without routing. This plan adds one route
to App.tsx and replaces the "Coming Soon" placeholder cards on SkillsPage with real
drill entry points for all 8 Tier A techniques. Then a human verifies the full flow.

Output:
- src/App.tsx — one new route added
- src/pages/SkillsPage.tsx — updated with 8 drill cards
</objective>

<execution_context>
@C:\Users\randy\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\randy\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@src/App.tsx
@src/pages/SkillsPage.tsx
@src/features/drill-engine/index.ts
@src/data/drill-techniques.ts

Key patterns:
- Routing pattern: see existing LabelingPracticeRoute and AuditPracticeRoute in App.tsx — mirror this pattern
- SkillsPage: currently has 2 active drill cards (labeling, accusation-audit) + 4 "Coming Soon" cards
- In Phase 20: replace the 4 "Coming Soon" cards + add the remaining 4 techniques as active cards
  Total: 8 active drill cards (all Tier A techniques)
- Keep existing labeling and accusation-audit cards pointing to their existing routes
  (/practice/labeling and /practice/audit) — DO NOT redirect them to the new generic engine
- The new generic engine route: /practice/drill/:techniqueId
- New techniques (6 new active cards): mirroring, open-ended-question, i-statement,
  no-oriented-question, nvc-observation, nvc-feeling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /practice/drill/:techniqueId route to App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Make two minimal changes to App.tsx:

    1. ADD import at top (near other feature imports):
       import { DrillScreen } from './features/drill-engine';

    2. ADD route wrapper function (near LabelingPracticeRoute):
       function DrillScreenRoute() {
         const { techniqueId } = useParams<{ techniqueId: string }>();
         const navigate = useNavigate();
         if (!techniqueId) return null;
         return <DrillScreen techniqueId={techniqueId} onBack={() => navigate('/skills-lab')} />;
       }

       Note: useParams is already imported from react-router-dom (check existing imports and add
       to the import statement if not already present).

    3. ADD route in the "Immersive routes" section (where /practice/labeling is):
       <Route path="/practice/drill/:techniqueId" element={<DrillScreenRoute />} />

    That is the complete change. Three additions:
    - One import line
    - One wrapper function
    - One Route element

    Do NOT modify any other routes. Do NOT change the TabLayout routes.
    Do NOT move existing /practice/labeling or /practice/audit routes.
  </action>
  <verify>
    Run: npm run type-check
    Expected: 0 errors

    Run: npm run build
    Expected: Build succeeds

    Manual check: route /practice/drill/mirroring would render DrillScreen with techniqueId='mirroring'
    Manual check: useParams extracts techniqueId correctly (string)
  </verify>
  <done>
    - src/App.tsx imports DrillScreen from './features/drill-engine'
    - DrillScreenRoute function defined using useParams
    - Route /practice/drill/:techniqueId added to immersive routes section
    - All existing routes unchanged (verify no accidental modifications)
    - npm run type-check passes, npm run build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SkillsPage.tsx — 8 active drill cards</name>
  <files>src/pages/SkillsPage.tsx</files>
  <action>
    Update SkillsPage to show 8 active drill cards — one per Tier A technique.

    KEEP existing cards:
    - "Labeling" card → navigate('/practice/labeling') — UNCHANGED
    - "Accusation Audit" card → navigate('/practice/audit') — UNCHANGED

    ADD 6 new drill cards (use navigate('/practice/drill/:techniqueId')):
    Use drillTechniquesArray imported from src/data/drill-techniques.ts.

    APPROACH — replace the "Coming Soon" section with a data-driven grid:

    Add import:
    import { drillTechniquesArray } from '../data/drill-techniques';

    The 6 new techniques to add as active cards:
    Filter drillTechniquesArray to exclude 'labeling' and 'accusation-audit' (which have their own routes).
    Render the remaining 6 as active drill cards in a separate grid or alongside the existing 2.

    CARD DESIGN for new generic engine cards:
    Same card-surface style as existing cards.
    Use border-l-4 border-l-accent for all (consistent — not per-card color).
    Show: technique name, technique description (first sentence only — slice at first period),
    framework badge (Voss/MI/CBT/NVC) in text-caption.
    onClick: navigate(`/practice/drill/${technique.id}`)
    Arrow indicator: → on right

    LAYOUT CHANGE:
    Replace the "Coming Soon" section title and cards with the 6 new active cards.
    Keep the "Practice Drills" section with the 2 existing cards (labeling + accusation-audit).
    Add the 6 new cards either:
      a) In the same "Practice Drills" grid (expanding it to a 3-column grid on lg screens)
      b) OR in a new section titled "Generic Drill Engine" — PREFER option (b) to keep sections clean

    RECOMMENDED LAYOUT:

    Section 1: "Practice Drills" (existing 2 cards — labeling, accusation-audit, unchanged)
    Section 2: "Technique Drills" (6 new generic engine cards — auto-generated from drillTechniquesArray)
    Section 3: "Technique Library" (existing button — unchanged)

    Remove the old "Coming Soon" section entirely.

    FRAMEWORK BADGE helper (inline function):
    const frameworkColor = (fw: string) => ({
      Voss: 'text-accent',
      MI: 'text-info',
      CBT: 'text-status-success',
      NVC: 'text-status-warning'
    }[fw] ?? 'text-text-muted')
  </action>
  <verify>
    Run: npm run type-check
    Expected: 0 errors in SkillsPage.tsx

    Run: npm run build
    Expected: 0 errors

    Manual check: SkillsPage renders 8 navigable technique cards (2 existing + 6 new)
    Manual check: "Coming Soon" section is gone
    Manual check: Labeling card still links to /practice/labeling (not /practice/drill/labeling)
  </verify>
  <done>
    - SkillsPage imports drillTechniquesArray from ../data/drill-techniques
    - 6 new drill cards rendered for mirroring, open-ended-question, i-statement,
      no-oriented-question, nvc-observation, nvc-feeling
    - Each new card navigates to /practice/drill/:technique.id
    - "Coming Soon" section removed
    - Existing labeling and accusation-audit cards unchanged
    - Technique Library button unchanged
    - npm run type-check passes, npm run build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 20 drill engine:
    - Form scoring (rules-based, instant) + Gemini accuracy/impact eval (async)
    - useDrillSession hook with state machine + attempt persistence
    - DrillScreen UI with Web Speech API recording + waveform
    - FeedbackCard with 3-dimension score breakdown + coaching
    - 8 drill entry points from SkillsPage
    - Route /practice/drill/:techniqueId in App.tsx
  </what-built>
  <how-to-verify>
    Open https://kdenz.vercel.app (or localhost:5173) in Chrome.

    1. SKILLS LAB NAVIGATION
       - Navigate to Skills Lab tab
       - Verify 8+ drill cards visible (not "Coming Soon" cards)
       - Verify "Labeling" and "Accusation Audit" cards still link to their existing routes

    2. DRILL LAUNCH — Mirroring
       - Tap "Mirroring" card in Skills Lab
       - Verify: URL changes to /practice/drill/mirroring
       - Verify: Technique name "Mirroring" appears in heading (Cormorant font)
       - Verify: Syntax template reminder visible ("Template: Back of [last 1-3 words]")
       - Verify: Scenario prompt displayed in a card
       - Verify: "Tap to Record Response" button (gold) visible

    3. RECORDING FLOW
       - Tap "Tap to Record Response"
       - Verify: Browser asks for mic permission (first time)
       - Speak a mirroring response: "Back of not sure what to do"
       - Verify: Live transcript appears while speaking
       - Verify: Waveform animation plays (5 animated bars)
       - Tap "Stop & Submit"

    4. SCORING FEEDBACK
       - Verify: "Scoring your response..." spinner appears briefly
       - Verify: FeedbackCard appears with Form score
       - Verify: If Gemini API key is configured: Accuracy and Impact scores appear within 5 seconds
       - Verify: If no API key: "Accuracy: — (Pending)" shown, form score displayed
       - Verify: Score labels match tier (e.g., 85% Form = "Well-formed")
       - Verify: Coaching narrative text visible (3-4 sentences)
       - Verify: Attempt count shows "Attempt 1"

    5. NEXT SCENARIO
       - Tap "Next Scenario"
       - Verify: New scenario prompt loads (different from first)
       - Verify: Attempt count shows "Attempt 1" (reset for new scenario — or increments: "Attempt 2" — check this matches hook behavior)
       - Verify: Back button returns to /skills-lab

    6. SECOND TECHNIQUE — Labeling (regression)
       - Navigate to Skills Lab
       - Tap "Labeling" card
       - Verify: Routes to /practice/labeling (existing feature, NOT generic engine)
       - Verify: Existing labeling practice UI renders correctly

    7. ERROR HANDLING
       - Test with empty speech (don't say anything, tap Stop immediately)
       - Verify: "Inaudible response. Please speak clearly and try again." appears
       - Verify: Retry is possible without leaving the screen

    8. NON-CHROME (optional if you have another browser available)
       - Open URL in Firefox or Safari
       - Verify: Chrome warning banner appears at top (not blocking modal)
       - Verify: Drill still renders below the banner
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark Phase 20 complete.
    Or describe issues for gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
After auto tasks complete (before human verify):
1. npm run type-check — 0 errors
2. npm run build — 0 errors
3. /practice/drill/:techniqueId route exists in App.tsx immersive routes section
4. SkillsPage renders 6 new technique cards from drillTechniquesArray
5. "Coming Soon" section removed from SkillsPage
6. Existing routes (/practice/labeling, /practice/audit) unchanged
</verification>

<success_criteria>
All Phase 20 requirements satisfied:
- ENG-01: User can practice any of 8 Tier A techniques via Prompt-Response drill
- ENG-02: Engine renders drills from data — no hardcoded per-technique UI
- ENG-03: Random scenario from technique pool each round
- ENG-04: Immediate feedback with per-dimension breakdown
- ENG-05: User can do multiple rounds (Next Scenario flow)
- SCR-01: Form scored via rules-based evaluation (regex/negation/inclusion)
- SCR-02: Accuracy/Impact via Gemini with structured rubric
- SCR-03: 4-dimension scores recorded (form 0.25, accuracy 0.35, impact 0.30, timing 0.10)
- SCR-04: Graceful degradation when Gemini unavailable

Human verify: approved
</success_criteria>

<output>
After human approves:
Create .planning/phases/20-drill-engine-scoring/20-04-SUMMARY.md with:
- What was built (routing + SkillsPage)
- Human verify result
- Any issues found and resolved
- Status: COMPLETE

Then update progress.txt with RESUME BLOCK for Phase 21.
</output>
