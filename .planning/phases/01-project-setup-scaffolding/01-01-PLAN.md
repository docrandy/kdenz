---
phase: 01-project-setup-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.js
  - src/index.css
  - src/App.tsx
  - src/main.tsx
  - src/components/PracticeSession.tsx
  - index.html
  - vite.config.ts
  - tsconfig.json
  - postcss.config.js
autonomous: true

must_haves:
  truths:
    - "Project builds successfully with npm run build"
    - "Dev server runs with npm run dev"
    - "App displays white background with teal accent visible"
    - "PracticeSession component renders placeholder UI"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "voicelab"
    - path: "tailwind.config.js"
      provides: "High-Performance Clinical theme"
      contains: "#00D4FF"
    - path: "src/components/PracticeSession.tsx"
      provides: "Practice session shell component"
      min_lines: 20
    - path: "src/App.tsx"
      provides: "Root application component"
      contains: "PracticeSession"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/PracticeSession.tsx"
      via: "import and render"
      pattern: "import.*PracticeSession"
    - from: "tailwind.config.js"
      to: "src/index.css"
      via: "Tailwind directives"
      pattern: "@tailwind"
---

<objective>
Initialize VoiceLab project with React + Vite + Tailwind, configured with the High-Performance Clinical design system (white/black/teal), and create the PracticeSession component shell.

Purpose: Establish the foundation for all subsequent development. Without a working build and design system, no features can be added.

Output: A buildable, runnable React application with the correct theme and a PracticeSession placeholder component ready for audio integration.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source codebase for reference (copy patterns, strip negotiation features):
- C:\Users\randy\.claude\projects\bLACK SwaN\package.json
- C:\Users\randy\.claude\projects\bLACK SwaN\src\App.tsx
- C:\Users\randy\.claude\projects\bLACK SwaN\tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize VoiceLab project structure</name>
  <files>
    package.json
    index.html
    vite.config.ts
    tsconfig.json
    postcss.config.js
    src/main.tsx
    src/App.tsx
    src/App.css
    src/index.css
    .gitignore
    .env.example
  </files>
  <action>
    Create a fresh Vite + React + TypeScript project for VoiceLab.

    Use Black Swan's package.json as reference but STRIP:
    - firebase and firebase-admin dependencies
    - stripe dependencies
    - body-parser, cors, express (backend stuff)
    - persona scripts
    - semantic-release dependencies

    KEEP (essential):
    - react, react-dom, react-router-dom
    - @google/genai (for Gemini AI summaries)
    - tailwindcss, autoprefixer, postcss
    - vite, vitest, typescript
    - @vitejs/plugin-react
    - @testing-library packages
    - eslint packages

    Create standard Vite project structure:
    - index.html with title "VoiceLab - Voice Practice"
    - vite.config.ts with React plugin
    - tsconfig.json for strict TypeScript
    - postcss.config.js for Tailwind

    Create src/main.tsx that renders App.
    Create src/App.tsx as placeholder that imports App.css.
    Create src/index.css with Tailwind directives (@tailwind base/components/utilities).
    Create .gitignore (node_modules, dist, .env, .env.local).
    Create .env.example with VITE_GEMINI_API_KEY placeholder.
  </action>
  <verify>
    npm install completes without errors.
    npm run dev starts the dev server.
    npm run build completes successfully.
  </verify>
  <done>
    Project folder has all config files, dependencies install, and app builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure High-Performance Clinical theme</name>
  <files>
    tailwind.config.js
    src/index.css
    src/App.css
  </files>
  <action>
    Update tailwind.config.js with High-Performance Clinical design system:

    ```javascript
    theme: {
      extend: {
        colors: {
          // High-Performance Clinical palette
          clinical: {
            bg: '#FFFFFF',        // white background
            text: '#000000',      // black text
            accent: '#00D4FF',    // electric teal/cyan
            'accent-hover': '#00B8E6', // slightly darker teal for hover
            muted: '#6B7280',     // gray for secondary text
            border: '#E5E7EB',    // light gray for borders
          }
        },
        fontFamily: {
          sans: ['Inter', 'system-ui', 'sans-serif'],
        }
      }
    }
    ```

    Update src/index.css to set body defaults:
    - Background: white
    - Font: Inter or system-ui fallback
    - Antialiased text rendering

    Clean up src/App.css - remove Vite defaults, keep minimal or empty.

    NO dark mode classes or media queries - light mode only (UI-02).
  </action>
  <verify>
    Run npm run dev.
    Open browser - page should show white background.
    Inspect element - verify Tailwind classes apply.
    Check that accent color (#00D4FF) is available via Tailwind (e.g., bg-clinical-accent).
  </verify>
  <done>
    Tailwind config has clinical colors. Body has white background and correct font.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PracticeSession component shell</name>
  <files>
    src/components/PracticeSession.tsx
    src/App.tsx
  </files>
  <action>
    Create src/components/ directory.

    Create src/components/PracticeSession.tsx as a shell component:
    - Functional component with TypeScript
    - Display a centered card with:
      - Title: "Voice Practice Session"
      - Subtitle: "Ready for audio integration"
      - A placeholder "Start Session" button (black background, white text)
      - Button should log to console when clicked (placeholder behavior)
    - Use Tailwind classes with clinical theme colors
    - Include a visual accent element (teal border or accent color somewhere)

    Update src/App.tsx to:
    - Import and render PracticeSession
    - Wrap in a full-height container (min-h-screen)
    - Center the content vertically and horizontally

    This is a SHELL - no actual audio functionality yet. Just visual structure.
  </action>
  <verify>
    npm run dev shows the PracticeSession component.
    Start Session button is visible with black background.
    Clicking button logs to console.
    Teal accent color visible somewhere (border, icon, or decorative element).
    Layout is centered on the page.
  </verify>
  <done>
    PracticeSession.tsx exists and renders. App.tsx displays it centered. Theme colors visible.
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. `npm install` - no errors
2. `npm run build` - builds successfully
3. `npm run dev` - server starts, page loads
4. Visual check: white background, black button, teal accent visible
5. PracticeSession component displays centered placeholder UI
</verification>

<success_criteria>
- Project structure matches Vite + React + TypeScript standard
- All dependencies install without conflicts
- Build succeeds without TypeScript errors
- High-Performance Clinical theme visible (white bg, black button, teal accent)
- PracticeSession shell component renders and is ready for audio integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-scaffolding/01-01-SUMMARY.md`
</output>
