---
phase: 10-privacy-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PreSessionScreen.tsx
  - src/pages/Settings.tsx
autonomous: true

must_haves:
  truths:
    - "User can see optional speaking prompts on pre-session screen"
    - "User can select a prompt before starting a session"
    - "User can skip prompts and start session without selecting one"
    - "Privacy page is accessible from Settings"
  artifacts:
    - path: "src/pages/PreSessionScreen.tsx"
      provides: "Speaking prompt selection UI"
      contains: "PromptSelector"
    - path: "src/pages/Settings.tsx"
      provides: "Privacy link in settings"
      contains: "/privacy"
  key_links:
    - from: "src/pages/PreSessionScreen.tsx"
      to: "src/components/PromptSelector.tsx"
      via: "import and render"
      pattern: "import.*PromptSelector"
---

<objective>
Wire existing speaking prompts UI into pre-session flow and add privacy link to Settings

Purpose: Complete Phase 10 by integrating already-built components. The Privacy page and PromptSelector component already exist — this plan wires them into the user flow.

Output: Users can optionally select speaking prompts before sessions, and privacy info is easily accessible from Settings.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-privacy-prompts/10-CONTEXT.md

# Existing components to integrate:
@src/components/PromptSelector.tsx
@src/data/speakingPrompts.ts
@src/pages/Privacy.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate PromptSelector into PreSessionScreen</name>
  <files>src/pages/PreSessionScreen.tsx</files>
  <action>
Import PromptSelector and SpeakingPrompt type from existing components.

Add state for selected prompt: `const [selectedPrompt, setSelectedPrompt] = useState<SpeakingPrompt | null>(null)`

Add PromptSelector component below the DurationSelector, passing:
- `onSelect={setSelectedPrompt}`
- `selectedDuration={selectedDuration}`

Pass selectedPrompt to the session via route state:
```typescript
navigate(`/practice/${mode}`, {
  state: {
    durationSeconds: selectedDuration,
    speakingPrompt: selectedPrompt
  }
});
```

The PromptSelector already handles expand/collapse and filtering by duration internally.
  </action>
  <verify>
npm run build (no TypeScript errors)
Visual: PreSessionScreen shows "+ Add a speaking prompt (optional)" button below duration picker
  </verify>
  <done>
PromptSelector visible on pre-session screen, expands to show prompts, selection updates state
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Privacy link to Settings page</name>
  <files>src/pages/Settings.tsx</files>
  <action>
Add a "Privacy & Data" section at the bottom of Settings, before the profile note.

Use Link from react-router-dom to navigate to /privacy:
```tsx
<section className="mb-8">
  <h2 className="text-sm font-semibold text-gray-900 mb-4">Privacy & Data</h2>
  <Link
    to="/privacy"
    className="flex items-center justify-between p-3 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors"
  >
    <div className="flex items-center gap-3">
      <svg className="w-5 h-5 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
      </svg>
      <div>
        <p className="text-sm font-medium text-gray-900">Privacy & Your Data</p>
        <p className="text-xs text-gray-500">How we handle your information</p>
      </div>
    </div>
    <svg className="w-4 h-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
    </svg>
  </Link>
</section>
```

This provides a second entry point to Privacy (in addition to hamburger menu) for users who naturally look in Settings.
  </action>
  <verify>
npm run build (no TypeScript errors)
Visual: Settings page shows "Privacy & Data" section with link to privacy page
  </verify>
  <done>
Privacy link visible in Settings, clicking navigates to /privacy page
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Build verification:
   ```bash
   npm run build
   ```
   Must exit 0 with no TypeScript errors.

2. Manual flow verification:
   - Navigate to Dashboard → Practice card → PreSessionScreen
   - Verify "+ Add a speaking prompt" button appears
   - Expand prompts, verify list shows
   - Select a prompt, verify it collapses with selection
   - Start session (prompt passed in state)
   - Navigate to Settings
   - Verify Privacy & Data section appears
   - Click Privacy link, verify navigation to /privacy
</verification>

<success_criteria>
- [ ] PromptSelector integrated into PreSessionScreen
- [ ] Prompts filter by selected duration
- [ ] Selected prompt passes to session via route state
- [ ] Privacy link added to Settings page
- [ ] Build passes with no errors
- [ ] All phase success criteria from ROADMAP.md satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-privacy-prompts/10-01-SUMMARY.md`
</output>
