---
phase: 15-new-screens-core-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WelcomeScreen.tsx
  - src/components/InlineProfileSetup.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "First-time users see full-viewport immersive hero with animated SessionOrb gently pulsing"
    - "Welcome screen shows aspirational premium copy and technique-focused 3-step intro"
    - "After tapping CTA, first-time users complete inline profile setup (name + goals) in under 30 seconds"
    - "Returning users auto-skip welcome and go straight to dashboard"
    - "Profile setup data is saved to localStorage via existing profileStorage"
  artifacts:
    - path: "src/components/WelcomeScreen.tsx"
      provides: "Redesigned immersive welcome hero with orb animation"
      min_lines: 60
    - path: "src/components/InlineProfileSetup.tsx"
      provides: "2-step inline profile setup (name + goals)"
      min_lines: 80
  key_links:
    - from: "src/components/WelcomeScreen.tsx"
      to: "src/components/SessionOrb.tsx"
      via: "import and render with idle pulsing animation"
      pattern: "import.*SessionOrb"
    - from: "src/components/InlineProfileSetup.tsx"
      to: "src/features/profile/profileStorage.ts"
      via: "saveProfile call to persist name and goals"
      pattern: "import.*saveProfile|import.*getProfile"
    - from: "src/App.tsx"
      to: "src/components/InlineProfileSetup.tsx"
      via: "shown after welcome CTA for first-time users"
      pattern: "InlineProfileSetup"
---

<objective>
Redesign the Welcome screen as an immersive full-viewport hero with animated SessionOrb and build inline profile setup for first-time users.

Purpose: SCR-01 requires a premium welcome experience that sets the tone for the app. User decision (LOCKED): full-viewport hero, animated orb pulsing, aspirational copy, technique-focused 3-step intro, auto-skip for returning users, inline profile setup (name + goals in 2 steps).

Output: Redesigned WelcomeScreen.tsx, new InlineProfileSetup.tsx, updated App.tsx flow.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-new-screens-core-flow/15-CONTEXT.md
@src/components/WelcomeScreen.tsx
@src/components/SessionOrb.tsx
@src/App.tsx
@src/features/profile/types.ts
@src/features/profile/profileStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign WelcomeScreen as immersive hero</name>
  <files>src/components/WelcomeScreen.tsx</files>
  <action>
Completely redesign the existing WelcomeScreen.tsx (currently a compact card layout) into a full-viewport immersive hero. Per user decisions (LOCKED):

**Layout:**
- Full viewport height (`min-h-screen`) with dark navy background
- No card container — content directly on background for immersive feel
- Vertically centered content with generous spacing

**SessionOrb centerpiece:**
- Import and render SessionOrb component in idle state
- Pass `audioLevel={0}`, `isRecording={false}`, `onClick={onStart}` (CTA triggers start)
- Add a gentle CSS pulse animation to the orb wrapper (Claude's discretion: subtle scale pulse, e.g., 1.0 to 1.03 over 3s ease-in-out infinite)
- The orb itself is the visual centerpiece — large, prominent

**Copy (aspirational premium tone — Claude can wordsmith):**
- Headline: Something like "Master Every Conversation" — use `font-display` (Cormorant Garamond), `text-display` size, `text-text-heading` color
- Subheadline: Brief aspirational line, `text-body-lg`, `text-text-body`
- 3-step intro (technique-focused per user decision):
  1. "Choose a technique" — with subtle icon or number
  2. "Practice with AI" — conversational practice
  3. "Get personalized coaching" — results and growth
- Style steps as minimal horizontal or vertical list with gold accent numbers, `text-body` size

**CTA Button:**
- Large gold button: "Begin Your Practice" or similar
- `btn-primary` styling, full width on mobile, max-width on desktop
- This triggers `onStart` prop (transitions to profile setup or dashboard)

**Bottom note:**
- Small "Beta" or "v2.0" badge, `text-caption`, `text-text-subtle`

**Auto-skip logic stays in App.tsx** (already implemented via WELCOME_SEEN_KEY). This component only renders when shown.
  </action>
  <verify>Build passes: `npm run build`. WelcomeScreen.tsx exists, imports SessionOrb, renders full-viewport layout.</verify>
  <done>Welcome screen is full-viewport immersive hero with animated SessionOrb, aspirational copy, 3-step technique intro, and gold CTA button.</done>
</task>

<task type="auto">
  <name>Task 2: Create InlineProfileSetup and integrate into onboarding flow</name>
  <files>src/components/InlineProfileSetup.tsx, src/App.tsx</files>
  <action>
Create a lightweight 2-step inline profile setup that appears after the welcome CTA for first-time users. Per user decisions (LOCKED): under 30 seconds total, 2 screens max.

**InlineProfileSetup.tsx:**

Props:
```tsx
interface InlineProfileSetupProps {
  onComplete: () => void;  // Called when setup is finished
  onSkip: () => void;      // Called if user skips
}
```

**Step 1: Name**
- Heading: "What should we call you?" (display font, heading color)
- Single text input for display name
- "Continue" button (gold, btn-primary)
- "Skip" link below

**Step 2: Goals (multi-select)**
- Heading: "What do you want to improve?" (display font, heading color)
- 6-8 goal options as tappable pills/chips (reuse GOAL_OPTIONS from profile types or create simplified inline set):
  - "Salary negotiations"
  - "Difficult conversations"
  - "Team leadership"
  - "Client relationships"
  - "Confidence building"
  - "Conflict handling"
  - "Boundary setting"
  - "Speaking up"
- Multi-select: tap to toggle, gold border when selected, max 3 recommended
- "Get Started" button (gold CTA)
- "Skip" link below

**Data persistence:**
- On complete, save to profile via `saveProfile()` from `src/features/profile/profileStorage.ts`
- Create or update profile: set `demographics.preferredName` and `focusAreas` / `longTermGoals`
- Use `getProfile()` to check if profile exists, merge data if so

**Layout:**
- Full-viewport centered, consistent with welcome screen dark background
- Smooth transition between steps (simple opacity fade or slide)
- Each step is a single focused screen — not a form with everything visible

**App.tsx integration:**
- After welcome (`handleWelcomeComplete`), check if profile has a name set
- If no profile name: show InlineProfileSetup instead of diagnostic/dashboard
- Add new state: `showProfileSetup`
- On InlineProfileSetup complete or skip: proceed to diagnostic check (existing flow)
- Store a `PROFILE_SETUP_SEEN_KEY` in localStorage to avoid re-showing

New flow: Consent → Welcome → Profile Setup (first time only) → Diagnostic → Dashboard
  </action>
  <verify>Build passes: `npm run build`. InlineProfileSetup.tsx exists. App.tsx has updated flow that shows profile setup after welcome for first-time users.</verify>
  <done>First-time users see 2-step inline profile setup after welcome (name + goals). Data persists to profileStorage. Returning users skip it automatically. Flow integrates seamlessly into existing onboarding sequence.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. WelcomeScreen renders full-viewport immersive hero with SessionOrb
3. SessionOrb gently pulses on welcome screen (CSS animation)
4. InlineProfileSetup shows 2 steps (name, goals) and saves to profile
5. First-time flow: Welcome → Profile Setup → Diagnostic → Dashboard
6. Returning users auto-skip welcome (existing localStorage check)
7. Profile setup can be skipped (Skip link on each step)
</verification>

<success_criteria>
- Welcome screen is premium immersive hero, not a compact card
- SessionOrb pulses gently as visual centerpiece
- Copy is aspirational and technique-focused
- Profile setup completes in 2 screens, under 30 seconds
- Data persists to localStorage via profileStorage
- No regression to returning user flow
</success_criteria>

<output>
After completion, create `.planning/phases/15-new-screens-core-flow/15-02-SUMMARY.md`
</output>
