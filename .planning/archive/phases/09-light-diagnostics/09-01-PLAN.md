---
phase: 09-light-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/geminiService.ts
  - src/components/AISummary.tsx
  - src/pages/PostSessionResults.tsx
autonomous: true

must_haves:
  truths:
    - "AI summary prompt includes user's speaking goals and challenges from diagnostic"
    - "AI coaching is personalized based on what user said matters to them"
    - "Local fallback still works when no API key (graceful degradation preserved)"
  artifacts:
    - path: "src/services/geminiService.ts"
      provides: "AI prompt with diagnostic context"
      contains: "diagnosticContext"
    - path: "src/components/AISummary.tsx"
      provides: "Diagnostic insights passed to summary generation"
      contains: "loadDiagnosticResults"
  key_links:
    - from: "src/components/AISummary.tsx"
      to: "src/lib/diagnosticQuestions.ts"
      via: "loadDiagnosticResults + summarizeInsights"
      pattern: "summarizeInsights"
    - from: "src/services/geminiService.ts"
      to: "AI prompt"
      via: "diagnosticContext parameter"
      pattern: "USER PROFILE"
---

<objective>
Wire diagnostic insights into AI summary generation so coaching is personalized based on what users said matters to them.

Purpose: DIAG-03 requires diagnostic context to be passed to AI summary. The infrastructure exists (summarizeInsights function) but is not connected to the Gemini prompt.

Output: AI summaries that reference user's stated goals and challenges, making the experience feel personalized ("this app gets me").
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/diagnosticQuestions.ts
@src/services/geminiService.ts
@src/components/AISummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostic context to Gemini prompt</name>
  <files>src/services/geminiService.ts</files>
  <action>
Update the geminiService.ts to accept and use diagnostic context in the AI prompt:

1. Add `diagnosticContext?: string` to the SummaryRequest interface
2. Modify `buildPrompt()` to include a USER PROFILE section when diagnosticContext is provided:
   ```
   ${diagnosticContext ? `
USER PROFILE:
${diagnosticContext}

Consider the user's stated goals and challenges when providing feedback. Reference what matters to them.
` : ''}
   ```
3. Place this section BEFORE the EVALUATION CRITERIA section
4. Keep the prompt concise - the diagnostic context is 1-2 sentences

The prompt should feel personalized without being verbose. Example:
"User is aware of filler word usage and wants to reduce it. Professional context - stakes feel high. Developing awareness, real-time feedback will help. Concrete metric-focused goal."
  </action>
  <verify>
1. `npx tsc --noEmit` passes with zero errors
2. grep for "diagnosticContext" in geminiService.ts shows the new parameter
3. grep for "USER PROFILE" in geminiService.ts shows the prompt section
  </verify>
  <done>Gemini prompt includes optional diagnostic context section that personalizes coaching feedback</done>
</task>

<task type="auto">
  <name>Task 2: Wire diagnostic insights into AISummary component</name>
  <files>src/components/AISummary.tsx</files>
  <action>
Update AISummary.tsx to load and pass diagnostic insights:

1. Import `loadDiagnosticResults` and `summarizeInsights` from '../lib/diagnosticQuestions'
2. In the `generateSummary` callback, before calling `generateCoachingSummary`:
   - Load diagnostic results: `const diagnosticResults = loadDiagnosticResults()`
   - Generate insights: `const diagnosticContext = summarizeInsights(diagnosticResults)`
3. Pass `diagnosticContext` to `generateCoachingSummary` in the request object
4. No UI changes needed - this is internal wiring only

The diagnostic context is loaded fresh each time (in case user retakes diagnostic).
  </action>
  <verify>
1. `npx tsc --noEmit` passes with zero errors
2. grep for "loadDiagnosticResults" in AISummary.tsx shows import
3. grep for "summarizeInsights" in AISummary.tsx shows usage
4. grep for "diagnosticContext" in AISummary.tsx shows it's passed to the request
  </verify>
  <done>AISummary loads diagnostic insights and passes them to AI summary generation</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end integration</name>
  <files>src/pages/PostSessionResults.tsx</files>
  <action>
Verify the integration works end-to-end:

1. Confirm PostSessionResults.tsx uses AISummary component (no changes needed if already present)
2. Run `npm run build` to verify production build passes
3. Verify the flow: diagnostic → localStorage → AISummary → geminiService → prompt

No code changes expected - this is verification that the chain is complete.

Document the integration path in the summary for future reference.
  </action>
  <verify>
1. `npm run build` succeeds with exit code 0
2. AISummary is rendered in PostSessionResults
3. Integration chain verified: diagnosticQuestions.ts → AISummary.tsx → geminiService.ts
  </verify>
  <done>End-to-end integration verified - diagnostic context flows from storage through to AI prompt</done>
</task>

</tasks>

<verification>
After all tasks:
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Production build passes: `npm run build`
3. Diagnostic context is wired into AI prompt (grep verifications)
4. Graceful degradation preserved (local fallback still works without API key)
</verification>

<success_criteria>
1. DIAG-03 complete: Diagnostic context passed to AI summary generation
2. AI prompt includes USER PROFILE section with diagnostic insights
3. Personalized coaching references user's stated goals when AI is enabled
4. No breaking changes to existing functionality (graceful degradation preserved)
5. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-light-diagnostics/09-01-SUMMARY.md`
</output>
