---
phase: 07-polish-error-handling
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PostSessionResults.tsx
  - src/pages/PreSessionScreen.tsx
  - src/pages/Dashboard.tsx
  - src/components/PracticeSession.tsx
  - src/components/Scorecard.tsx
autonomous: true

must_haves:
  truths:
    - "Mobile Chrome users can complete full session flow"
    - "Touch targets are at least 44x44px"
    - "No horizontal scroll on mobile viewport"
  artifacts: []
  key_links: []
---

<objective>
Polish mobile layout for Chrome mobile usability.

Purpose: Beta testers will use Chrome mobile - all flows must be usable on small screens.
Output: Responsive adjustments to key pages, touch-friendly controls, no layout breaks.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Dashboard.tsx
@src/pages/PostSessionResults.tsx
@src/components/Scorecard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile-responsive Dashboard and PreSession</name>
  <files>src/pages/Dashboard.tsx, src/pages/PreSessionScreen.tsx</files>
  <action>
Audit and fix mobile responsiveness on entry screens:

Dashboard.tsx:
1. Practice cards should stack vertically on mobile (already using grid)
2. Profile preview card: ensure text doesn't overflow
3. Session cards: ensure touch targets are 44px minimum height
4. Hamburger menu: ensure dropdown doesn't overflow viewport
5. Add safe-area-inset padding for notched phones:
   - Use `pb-safe` or `padding-bottom: env(safe-area-inset-bottom)`

PreSessionScreen.tsx:
1. Duration selector buttons: minimum 44x44px touch targets
2. Ensure button text is readable on small screens
3. Stack duration options 2x2 on very small screens (< 360px)
4. Add horizontal padding to prevent edge-to-edge content

Use Tailwind responsive prefixes: sm:, md:, lg:
Default styles should be mobile-first.
  </action>
  <verify>npm run build succeeds, visual check on 375px viewport shows no overflow</verify>
  <done>Dashboard and PreSessionScreen are mobile-responsive with proper touch targets</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-responsive PostSessionResults</name>
  <files>src/pages/PostSessionResults.tsx, src/components/Scorecard.tsx</files>
  <action>
Audit and fix mobile responsiveness on results screens:

PostSessionResults.tsx:
1. Navigation buttons: full width on mobile, side-by-side on larger screens
2. SelfAssessment rating buttons: ensure 44px touch targets
3. ImplementationIntention chip suggestions: wrap properly on mobile
4. Ensure comfortable reading width (max-w-md or similar)

Scorecard.tsx:
1. MetricCard grid: stack vertically on mobile (1 column)
2. Trend chart: ensure readable on 320px width
3. Font sizes: maintain readability (minimum 14px body text)
4. Spacing: reduce padding on mobile if needed

Common patterns to apply:
- `grid grid-cols-1 md:grid-cols-2` for card grids
- `text-sm md:text-base` for adaptive text
- `p-4 md:p-6` for adaptive padding
- `gap-3 md:gap-4` for adaptive spacing
  </action>
  <verify>npm run build succeeds, Scorecard readable on 375px viewport</verify>
  <done>PostSessionResults and Scorecard display correctly on mobile</done>
</task>

<task type="auto">
  <name>Task 3: Mobile-responsive PracticeSession</name>
  <files>src/components/PracticeSession.tsx</files>
  <action>
Audit and fix mobile responsiveness for active session:

1. SessionOrb:
   - Ensure orb doesn't overflow on small screens
   - Add responsive sizing if needed (smaller on mobile)

2. Filler counter / WPM display:
   - Centered, readable text size
   - Sufficient contrast on white background

3. BottomControlBar:
   - Ensure buttons have 44px+ touch targets
   - Adequate spacing between Pause/Stop buttons
   - Fixed at bottom of viewport (not scroll with content)

4. WaveformVisualizer:
   - Ensure canvas scales properly on mobile
   - May need to reduce height on very small screens

5. Baseline prompts:
   - Ensure text wraps properly
   - Readable font size (14-16px)

6. Add viewport meta tag check (should be in index.html):
   - `<meta name="viewport" content="width=device-width, initial-scale=1">`

Focus on touch interactions - no hover-only features on core controls.
  </action>
  <verify>npm run build succeeds, session flow usable on mobile Chrome</verify>
  <done>PracticeSession fully usable on Chrome mobile</done>
</task>

</tasks>

<verification>
- [ ] npm run build completes without errors
- [ ] npm run lint passes
- [ ] No horizontal scroll on 375px viewport
- [ ] All interactive elements have 44px+ touch targets
- [ ] Text remains readable on mobile
</verification>

<success_criteria>
- Mobile Chrome users can complete full session flow without issues
- Touch targets meet accessibility guidelines (44x44px minimum)
- No horizontal overflow on any page
- Layout adapts gracefully from mobile to desktop
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-error-handling/07-04-SUMMARY.md`
</output>
