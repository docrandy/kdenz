---
phase: 07-polish-error-handling
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/components/FeedbackButton.tsx
  - src/App.tsx
  - src/components/LoadingSpinner.tsx
  - src/pages/PostSessionResults.tsx
  - src/components/PracticeSession.tsx
autonomous: false

must_haves:
  truths:
    - "User can submit feedback via in-app button"
    - "Loading states show during async operations"
    - "Transitions feel smooth and intentional"
  artifacts:
    - path: "src/components/LoadingSpinner.tsx"
      provides: "Reusable loading spinner component"
      min_lines: 15
  key_links:
    - from: "src/App.tsx"
      to: "FeedbackButton"
      via: "global footer render"
      pattern: "<FeedbackButton"
---

<objective>
Polish feedback mechanism and add loading/transition states.

Purpose: Beta testers need easy feedback path. Loading states reduce perceived latency and show system responsiveness.
Output: Enhanced FeedbackButton, consistent LoadingSpinner, polished transitions.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/FeedbackButton.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance FeedbackButton with context</name>
  <files>src/components/FeedbackButton.tsx</files>
  <action>
Enhance the existing FeedbackButton component (already rendered in App.tsx global footer) based on research findings:

Research insight: In-app feedback with context has higher response rates than plain mailto.

1. Add session context to feedback:
   - Include current page/route in mailto body
   - Include recent session stats if available (from sessionStorage)
   - Format: "Page: {route}\nRecent session: {date}, {duration}s, {fillerCount} fillers"

2. Add feedback prompt after session:
   - New prop: `showPrompt?: boolean`
   - When true, show expanded state with "How was your session?" text
   - Auto-collapse after 10 seconds or on dismiss

3. Visual polish:
   - Ensure button doesn't overlap content on mobile
   - Add subtle pulse animation on first appearance (once per session)
   - Use clinical-text color for consistency

4. Accessibility:
   - Ensure aria-label is descriptive
   - Tab-navigable
   - Focus visible state

Keep mailto approach (simplest for 5-10 testers per research).
  </action>
  <verify>FeedbackButton renders with enhanced context, mailto includes page info</verify>
  <done>FeedbackButton includes session context and prompts after session completion</done>
</task>

<task type="auto">
  <name>Task 2: Create LoadingSpinner component</name>
  <files>src/components/LoadingSpinner.tsx</files>
  <action>
Create a reusable loading spinner component:

1. Props:
   - `size?: 'sm' | 'md' | 'lg'` (default: 'md')
   - `color?: string` (default: clinical-accent)
   - `className?: string`

2. Visual design:
   - CSS-only animation (no external dependencies)
   - Clean, clinical aesthetic
   - Three sizes: sm (16px), md (24px), lg (32px)
   - Use clinical-accent color by default

3. Animation:
   - Smooth rotation animation
   - Use Tailwind's animate-spin class
   - Circular spinner with partial arc

4. Usage example:
   ```tsx
   <LoadingSpinner size="md" />
   ```

Keep it simple - one purpose, done well.
  </action>
  <verify>Component renders, animation spins smoothly</verify>
  <done>LoadingSpinner component exists with three sizes and clinical styling</done>
</task>

<task type="auto">
  <name>Task 3: Add loading states to key flows</name>
  <files>src/pages/PostSessionResults.tsx, src/components/PracticeSession.tsx</files>
  <action>
Add loading indicators to async operations:

PostSessionResults.tsx:
1. AI Summary loading:
   - Show LoadingSpinner while Gemini API is processing
   - Already has isLoading state - wire up spinner display
   - Center spinner in summary area

2. Phase transitions:
   - Add subtle fade transitions between phases (self-assess -> metrics -> intention)
   - Use Tailwind transition-opacity duration-300

PracticeSession.tsx:
1. Session start loading (already has isStarting):
   - Ensure orb shows loading state clearly
   - Already implemented - verify it works

2. Session end:
   - Brief loading state while processing results
   - Show spinner during blob-to-base64 conversion

Add `transition-all duration-200` to page-level containers for smooth route transitions.
  </action>
  <verify>npm run build succeeds, loading states visible during async operations</verify>
  <done>Loading states and smooth transitions improve perceived responsiveness</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 07 polish changes including mobile responsive layouts, audio quality warnings, transcript confidence indicator, copy-lint script, feedback button enhancements, and loading states</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open Chrome DevTools and toggle mobile viewport (375px width)
3. Complete full session flow:
   a. Dashboard -> Select practice mode -> Duration picker -> Start session
   b. Speak for 30+ seconds with some fillers
   c. End session -> View results
   d. Check transcript on evaluation page
4. Verify on each screen:
   - No horizontal scroll
   - All buttons easily tappable (44px+)
   - Text readable
   - Loading states appear appropriately
5. Check feedback button:
   - Click and verify mailto includes page context
6. Run `npm run copy-lint` and review any violations
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build completes without errors
- [ ] npm run lint passes
- [ ] FeedbackButton includes session context
- [ ] LoadingSpinner component exists
- [ ] Loading states visible during AI summary generation
- [ ] Transitions are smooth between phases
</verification>

<success_criteria>
- Feedback button includes contextual information
- Loading states clearly indicate async operations
- Transitions feel polished and intentional
- Full session flow verified on mobile Chrome
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-error-handling/07-05-SUMMARY.md`
</output>
