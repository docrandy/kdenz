---
phase: 07-polish-error-handling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/audio/useWebSpeech.ts
  - src/components/TranscriptConfidenceIndicator.tsx
  - src/pages/PostSessionResults.tsx
  - src/pages/EvaluationPage.tsx
autonomous: true

must_haves:
  truths:
    - "User sees transcript confidence indicator before metrics"
    - "Low confidence shows warning about filler count accuracy"
    - "Confidence level clearly communicated without judgment"
  artifacts:
    - path: "src/components/TranscriptConfidenceIndicator.tsx"
      provides: "Transcript confidence display component"
      min_lines: 25
  key_links:
    - from: "src/pages/PostSessionResults.tsx"
      to: "TranscriptConfidenceIndicator"
      via: "render before Scorecard"
      pattern: "<TranscriptConfidenceIndicator"
---

<objective>
Add transcript confidence indicator to inform users about metric reliability.

Purpose: Per Core Principle #3 (uncertainty communication), users need to understand when transcription quality affects downstream metrics like filler counts.
Output: TranscriptConfidenceIndicator component, confidence tracking in useWebSpeech hook, indicator displayed on results page.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/foundation/copy/audio-quality-warning-messages-v1.md
@docs/foundation/product/language-boundaries-v1.md
@src/core/audio/useWebSpeech.ts
@src/pages/PostSessionResults.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confidence tracking to useWebSpeech hook</name>
  <files>src/core/audio/useWebSpeech.ts</files>
  <action>
Add confidence tracking to the Web Speech API hook:

1. Track confidence scores:
   - `confidenceScores: number[]` - all result confidence values (0-1)
   - `averageConfidence: number` - computed average (0-1)
   - `lowConfidenceSegments: number` - count of segments below 0.7 threshold

2. Capture confidence from results:
   - In onresult handler, result[0].confidence is the confidence score
   - Store each final result's confidence in confidenceScores array
   - Update averageConfidence on each final result

3. Expose in hook return:
   - Add `averageConfidence: number` to UseWebSpeechResult
   - Add `lowConfidenceSegments: number` to UseWebSpeechResult

4. Note: Chrome's Web Speech API may return 0 confidence for interim results.
   Only track confidence from isFinal results.

Reset all confidence tracking in start() function.
  </action>
  <verify>Hook compiles, averageConfidence and lowConfidenceSegments exposed</verify>
  <done>useWebSpeech hook tracks and exposes transcript confidence metrics</done>
</task>

<task type="auto">
  <name>Task 2: Create TranscriptConfidenceIndicator component</name>
  <files>src/components/TranscriptConfidenceIndicator.tsx</files>
  <action>
Create a component that displays transcript confidence status:

1. Props:
   - `averageConfidence: number` (0-1)
   - `lowSegmentCount: number`
   - `className?: string`

2. Display tiers (no judgment colors - use neutral clinical-accent):
   - High (>= 0.85): "Transcription confidence: High" - no warning
   - Medium (0.7-0.85): "Transcription confidence: Medium" - note about possible variation
   - Low (< 0.7): "Transcription confidence: Low" - full warning message

3. Low confidence warning (from foundation docs):
   "Low transcription confidence in this segment; filler counts may be under- or over-estimated."

4. Visual design:
   - Compact inline indicator (not a full banner)
   - Info icon (circle with "i")
   - Clinical-accent color for icon
   - Gray text for label
   - Expandable tooltip/help text on click for low/medium

5. Do NOT use:
   - Red/green/yellow colors for confidence levels (violates design principle #2)
   - Words like "good", "bad", "poor", "excellent"

Use neutral language: "High", "Medium", "Low" only.
  </action>
  <verify>Component renders, shows correct tier, no judgment colors used</verify>
  <done>TranscriptConfidenceIndicator displays confidence without judgment</done>
</task>

<task type="auto">
  <name>Task 3: Wire confidence indicator into results pages</name>
  <files>src/pages/PostSessionResults.tsx, src/pages/EvaluationPage.tsx</files>
  <action>
Add confidence indicator to post-session pages:

1. PostSessionResults.tsx:
   - Import TranscriptConfidenceIndicator
   - Read averageConfidence and lowConfidenceSegments from sessionStorage
   - Display indicator above Scorecard (in metrics phase)
   - Only show if averageConfidence < 0.85 (hide for high confidence)

2. EvaluationPage.tsx:
   - Import TranscriptConfidenceIndicator
   - Read confidence data from sessionStorage
   - Display indicator above TranscriptView
   - Only show if averageConfidence < 0.85

3. Update PracticeSession handleStop:
   - Include averageConfidence and lowConfidenceSegments in sessionStorage save
   - Data flows: hook -> handleStop -> sessionStorage -> results page

4. Positioning:
   - PostSessionResults: Between reflection phase and Scorecard
   - EvaluationPage: Above transcript section

Note: These files can be edited in parallel since they're independent.
  </action>
  <verify>npm run build succeeds, confidence indicator appears on results pages</verify>
  <done>Transcript confidence indicator appears on PostSessionResults and EvaluationPage</done>
</task>

</tasks>

<verification>
- [ ] npm run build completes without errors
- [ ] npm run lint passes
- [ ] TranscriptConfidenceIndicator component exists
- [ ] useWebSpeech exposes averageConfidence
- [ ] No judgment colors (red/green/yellow) used for confidence
- [ ] Warning copy matches foundation docs for low confidence
</verification>

<success_criteria>
- Transcript confidence is tracked during speech recognition
- Confidence indicator shows on results pages when confidence < 0.85
- Uses neutral language (High/Medium/Low) without judgment colors
- Low confidence warning uses verbatim foundation copy
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-error-handling/07-02-SUMMARY.md`
</output>
