---
phase: 08-deployment-beta-prep
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App works correctly on Chrome desktop"
    - "App works correctly on Chrome mobile"
    - "Non-Chrome users see browser warning"
    - "Testers have clear instructions for getting started"
  artifacts: []
  key_links:
    - from: "BrowserWarning.tsx"
      to: "App.tsx"
      via: "Browser detection conditional"
      pattern: "isChrome.*BrowserWarning"
---

<objective>
Test across devices and create tester onboarding for beta distribution.

Purpose: Verify the complete user experience and prepare materials for 5-10 beta testers.
Output: Verified app ready for distribution with tester instructions.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/BrowserWarning.tsx
@.planning/phases/08-deployment-beta-prep/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Chrome desktop verification</name>
  <what-built>Complete VoiceLab app deployed to production</what-built>
  <how-to-verify>
    Open the production URL in Chrome desktop and verify:
    1. Dashboard loads correctly
    2. Focus mode selection (Filler Words / Speech Pace) works
    3. Start a short session (1 minute)
    4. Verify real-time filler detection works
    5. Complete session and verify post-session results display
    6. Check scorecard metrics display correctly
    7. Test audio playback with filler markers
    8. Navigate to evaluation page and verify transcript displays
    9. Test AI Summary button (if Gemini API key configured)
    10. Verify feedback button opens email client

    Note any issues encountered.
  </how-to-verify>
  <resume-signal>Type "desktop approved" or describe issues to fix</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome mobile verification</name>
  <what-built>Mobile-responsive layout from Phase 07-04</what-built>
  <how-to-verify>
    Open the production URL on a mobile device with Chrome:
    1. Dashboard is responsive and readable
    2. Touch targets are large enough (44px+ minimum)
    3. Session controls work via touch
    4. SessionOrb displays correctly
    5. Post-session results scroll properly
    6. All buttons are tappable without precision
    7. No horizontal scrolling needed
    8. Safe area insets work on notched devices (if available)

    Note any mobile-specific issues.
  </how-to-verify>
  <resume-signal>Type "mobile approved" or describe issues to fix</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Non-Chrome browser warning verification</name>
  <what-built>BrowserWarning component from Phase 01</what-built>
  <how-to-verify>
    Open the production URL in a non-Chrome browser (Safari or Firefox):
    1. Browser warning displays prominently
    2. Message clearly explains Chrome is required
    3. User's current browser is correctly identified
    4. "Continue Anyway" button works (for testing purposes)
    5. Warning is dismissible

    Verify the warning provides clear guidance for testers.
  </how-to-verify>
  <resume-signal>Type "warning approved" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Create tester welcome message</name>
  <files>BETA_TESTERS.md</files>
  <action>
    Create a simple tester welcome file at project root (BETA_TESTERS.md) with:
    - Production URL
    - Chrome requirement (with brief explanation why)
    - Quick start: "Open URL in Chrome, allow microphone, pick a focus mode, talk for 1-3 minutes"
    - What to try: filler word mode, pace mode, audio playback, AI summary (if configured)
    - How to give feedback: use in-app button or email directly
    - Known limitations: Chrome only, no session history yet, local storage only

    Keep it casual and friendly - these are friends/family testers.
    One page max, scannable format (bullets, not paragraphs).
  </action>
  <verify>BETA_TESTERS.md exists at project root with clear instructions</verify>
  <done>Tester welcome message created</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Distribute to beta testers</name>
  <action>Send the production URL and welcome instructions to 5-10 beta testers</action>
  <instructions>
    Distribution is a human task - Claude cannot send emails or messages to testers.

    Share the production URL with your 5-10 testers. Options:
    1. Copy BETA_TESTERS.md content into an email/message
    2. Send just the URL with a quick "Open in Chrome, allow mic, try it out!"
    3. Share via group chat, text, or however you communicate with testers

    The lightest approach is fine - these are friends/family who agreed to help.
  </instructions>
  <resume-signal>Type "distributed" when URL has been sent to testers</resume-signal>
</task>

</tasks>

<verification>
- Chrome desktop: full session flow works
- Chrome mobile: responsive layout, touch targets work
- Non-Chrome: browser warning displays correctly
- Tester instructions: clear and simple
- Distribution: testers have received the URL
</verification>

<success_criteria>
- Desktop verification approved
- Mobile verification approved
- Browser warning verification approved
- Tester welcome message created
- URL distributed to 5-10 testers
</success_criteria>

<output>
After completion, create `.planning/phases/08-deployment-beta-prep/08-02-SUMMARY.md`
</output>
