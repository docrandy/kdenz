---
phase: 12-color-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WelcomeScreen.tsx
  - src/components/ConsentModal.tsx
  - src/components/BrowserWarning.tsx
  - src/components/DiagnosticOnboarding.tsx
  - src/components/PromptSelector.tsx
  - src/components/MicPermissionError.tsx
  - src/components/FeedbackButton.tsx
  - src/components/SelfAssessment.tsx
  - src/components/ImplementationIntention.tsx
  - src/components/SettingsPanel.tsx
  - src/components/QuickNoteBox.tsx
  - src/components/DevFeedbackBoxes.tsx
  - src/components/AudioQualityWarning.tsx
  - src/types/technique.ts
autonomous: true

must_haves:
  truths:
    - "WelcomeScreen renders on dark navy background with gold CTA button"
    - "ConsentModal displays on dark surface with proper text contrast"
    - "BrowserWarning shows on dark background with status-warning colors"
    - "DiagnosticOnboarding renders questions on dark surfaces"
    - "PromptSelector options display on dark surfaces with gold-selected state"
    - "MicPermissionError uses status-error colors on dark background"
    - "SettingsPanel renders on dark surface with proper form controls"
    - "All status badges use semantic status-* colors"
  artifacts:
    - path: "src/components/WelcomeScreen.tsx"
      provides: "Dark-themed welcome/landing screen"
      contains: "bg-background"
    - path: "src/components/ConsentModal.tsx"
      provides: "Dark-themed consent modal"
      contains: "bg-background-surface"
    - path: "src/components/SettingsPanel.tsx"
      provides: "Dark-themed settings panel"
      contains: "bg-background"
    - path: "src/types/technique.ts"
      provides: "Semantic status colors for difficulty levels"
      contains: "text-status"
  key_links:
    - from: "src/components/WelcomeScreen.tsx"
      to: "tailwind.config.js"
      via: "semantic color tokens"
      pattern: "bg-background|text-text|bg-accent"
    - from: "src/types/technique.ts"
      to: "tailwind.config.js"
      via: "status color tokens"
      pattern: "text-status"
---

<objective>
Migrate 13 onboarding, modal, settings, and utility components plus 1 type file from clinical light palette to dark premium palette.

Purpose: These components handle user onboarding (WelcomeScreen, DiagnosticOnboarding), permissions (ConsentModal, BrowserWarning, MicPermissionError), user input (SettingsPanel, SelfAssessment, PromptSelector), and utility displays (DevFeedbackBoxes, QuickNoteBox, AudioQualityWarning). The technique.ts type file contains hardcoded color strings for difficulty levels. This is the largest plan by file count but each file's migration is straightforward pattern replacement.

Output: 14 files migrated from clinical/white/gray/teal to dark premium semantic tokens.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-color-migration/12-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md
@.planning/phases/11-design-system-foundation/11-02-SUMMARY.md
@tailwind.config.js
@src/index.css
</context>

<migration_reference>
## Color Migration Mapping (apply to ALL files in this plan)

### Background replacements
- `bg-white` → `bg-background` (page-level) or `bg-background-surface` (card/panel)
- `bg-gray-50`, `bg-gray-100` → `bg-background-surface` or `bg-background-elevated`
- `bg-gray-200`, `bg-gray-300` → `bg-background-elevated`
- `bg-clinical-bg` → `bg-background`
- `bg-clinical-accent` → `bg-accent`

### Text replacements
- `text-gray-900`, `text-clinical-text`, `text-black` → `text-text`
- `text-gray-700`, `text-gray-600` → `text-text-muted`
- `text-gray-500`, `text-gray-400`, `text-clinical-muted` → `text-text-subtle`
- `text-white` (on dark bg) → `text-text`; (on accent bg) → `text-text-inverse`
- `text-clinical-accent` → `text-accent`

### Border replacements
- `border-gray-200`, `border-gray-300`, `border-clinical-border` → `border-background-elevated`
- `border-gray-100` → `border-background-surface`

### Status color replacements
- `text-red-*`, `bg-red-*` → `text-status-error`, `bg-status-error/10` or `bg-status-error/15`
- `text-green-*`, `bg-green-*` → `text-status-success`, `bg-status-success/10`
- `text-blue-*`, `bg-blue-*` → `text-status-info`, `bg-status-info/10`
- `text-yellow-*`, `text-amber-*`, `bg-yellow-*`, `bg-amber-*` → `text-status-warning`, `bg-status-warning/10`

### Interactive states
- `hover:bg-gray-50`, `hover:bg-gray-100` → `hover:bg-background-elevated`
- `hover:bg-gray-200` → `hover:bg-background-subtle`
- `focus:ring-blue-*` → `focus:ring-accent`

### Per CONTEXT decisions
- Gold accent sparingly — primary CTAs only (WelcomeScreen "Start" button)
- Secondary interactive: cream/white tones
- Modal overlays: `bg-black/50` backdrop stays (dark overlay is fine)
- Status background tints: use `/10` or `/15` opacity for tinted surfaces on dark
- Disabled states: `opacity-50` approach (per CONTEXT discretion)
</migration_reference>

<tasks>

<task type="auto">
  <name>Task 1: Migrate WelcomeScreen, ConsentModal, BrowserWarning, DiagnosticOnboarding</name>
  <files>src/components/WelcomeScreen.tsx, src/components/ConsentModal.tsx, src/components/BrowserWarning.tsx, src/components/DiagnosticOnboarding.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**WelcomeScreen.tsx** (~22 old refs): First screen users see.
- This is a visually important screen — sets the premium tone
- Page background: `bg-background` (dark navy)
- Main heading: `text-text` (cream) — consider `text-gradient-gold` for the app name
- Subheading: `text-text-muted`
- Primary CTA ("Start Practice" or similar): `btn-primary` (gold, the only gold element)
- Secondary links: `text-text-muted hover:text-text` (cream, NOT gold)
- Replace all `bg-white` → `bg-background`
- Replace all `text-clinical-*` / `text-gray-*` / `text-black` → appropriate `text-text-*`
- Replace `bg-clinical-accent` / `bg-black` buttons → `btn-primary` or `bg-accent text-text-inverse`
- Feature cards/sections: `bg-background-surface border border-background-elevated`

**ConsentModal.tsx** (~18 old refs): Data consent modal overlay.
- Modal backdrop: `bg-black/50` (keep dark overlay)
- Modal panel: `bg-background-surface border border-background-elevated rounded-xl`
- Title: `text-text`
- Body text: `text-text-muted`
- Checkboxes/labels: `text-text`
- Accept button: `btn-primary` (gold CTA)
- Decline/close: `btn-ghost` or `text-text-muted hover:text-text`
- Replace all `bg-white` → `bg-background-surface`
- Replace all `text-gray-*` / `text-clinical-*` → appropriate variants

**BrowserWarning.tsx** (~7 old refs): Non-Chrome browser warning.
- Warning container: `bg-status-warning/10 border border-status-warning/30 rounded-lg`
- Icon/title: `text-status-warning`
- Description: `text-text-muted`
- Replace `bg-yellow-*`/`bg-amber-*` → `bg-status-warning/10`
- Replace `text-yellow-*`/`text-amber-*` → `text-status-warning`
- Replace `bg-white` → `bg-background-surface`

**DiagnosticOnboarding.tsx** (~9 old refs): 3-5 question onboarding flow.
- Container: `bg-background`
- Question cards: `bg-background-surface border border-background-elevated`
- Question text: `text-text`
- Options: `bg-background-elevated text-text-muted` → selected: `bg-accent text-text-inverse`
- Progress indicator: `bg-accent` on `bg-background-elevated` track
- Replace all `text-gray-*` → appropriate variants
- Replace `bg-clinical-*` → semantic equivalents
  </action>
  <verify>Grep all 4 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `text-black`, `bg-black` (except backdrop overlays) — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>WelcomeScreen, ConsentModal, BrowserWarning, and DiagnosticOnboarding contain zero old palette references. WelcomeScreen uses gold CTA. ConsentModal uses dark surface. BrowserWarning uses status-warning.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PromptSelector, MicPermissionError, FeedbackButton, SelfAssessment, ImplementationIntention</name>
  <files>src/components/PromptSelector.tsx, src/components/MicPermissionError.tsx, src/components/FeedbackButton.tsx, src/components/SelfAssessment.tsx, src/components/ImplementationIntention.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**PromptSelector.tsx** (~15 old refs): Speaking prompt selection.
- Container: `bg-background-surface`
- Prompt cards: `bg-background-elevated border border-background-subtle` → selected: `bg-accent/10 border-accent text-text`
- Prompt titles: `text-text`
- Prompt descriptions: `text-text-muted`
- "Free Practice" / "Skip" option: `bg-background-elevated text-text-muted`
- Replace all `bg-white`/`bg-clinical-*` → semantic equivalents
- Replace all `text-gray-*` → appropriate variants

**MicPermissionError.tsx** (~11 old refs): Microphone permission denied screen.
- Container: `bg-background`
- Error icon: `text-status-error`
- Error title: `text-text`
- Error description: `text-text-muted`
- Steps/instructions: `bg-background-surface border border-background-elevated`
- "Try Again" button: `btn-primary` (gold CTA)
- Replace `text-red-*` → `text-status-error`
- Replace `bg-red-*` → `bg-status-error/10`

**FeedbackButton.tsx** (~5 old refs): In-app feedback trigger.
- Button: `bg-background-surface text-text-muted border border-background-elevated`
- Hover: `hover:bg-background-elevated hover:text-text`
- Replace `bg-white` / `bg-gray-*` → `bg-background-surface`
- Replace `text-gray-*` → `text-text-muted`

**SelfAssessment.tsx** (~13 old refs): Self-rating component.
- Container: `bg-background-surface`
- Rating labels: `text-text-muted`
- Selected rating: `bg-accent text-text-inverse` (gold selected) or status colors for good/bad
- Unselected: `bg-background-elevated text-text-muted`
- Replace all `bg-white` → `bg-background-surface`
- Replace all `text-gray-*` → appropriate variants

**ImplementationIntention.tsx** (~17 old refs): Goal-setting component.
- Container: `bg-background-surface border border-background-elevated`
- Section headers: `text-text`
- Input fields: use `input` class (from design system) or `bg-background-surface border border-background-elevated text-text`
- Placeholder text: `text-text-subtle`
- Submit button: `btn-primary` (gold CTA)
- Help text: `text-text-subtle`
- Replace all `bg-white` → `bg-background-surface`
- Replace all `text-clinical-*`/`text-gray-*` → appropriate variants
  </action>
  <verify>Grep all 5 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `text-red`, `bg-red` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>PromptSelector, MicPermissionError, FeedbackButton, SelfAssessment, and ImplementationIntention contain zero old palette references. Gold used for primary CTAs and selected states. Status-error used for permission errors.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SettingsPanel, QuickNoteBox, DevFeedbackBoxes, AudioQualityWarning, technique types</name>
  <files>src/components/SettingsPanel.tsx, src/components/QuickNoteBox.tsx, src/components/DevFeedbackBoxes.tsx, src/components/AudioQualityWarning.tsx, src/types/technique.ts</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**SettingsPanel.tsx** (~26 old refs): Settings/configuration panel.
- Container: `bg-background-surface border border-background-elevated`
- Section headers: `text-text`
- Labels: `text-text-muted`
- Input fields: use `input` class or `bg-background-elevated border border-background-subtle text-text`
- Toggle switches: active = `bg-accent`, inactive = `bg-background-elevated`
- Replace ALL `bg-white` → `bg-background-surface`
- Replace ALL `text-gray-*` → appropriate variants
- Replace ALL `border-gray-*` → `border-background-elevated`
- Replace ALL `clinical-*` → semantic equivalents
- This file has 26 references — be thorough

**QuickNoteBox.tsx** (~12 old refs): Quick note input box.
- Container: `bg-background-surface border border-background-elevated`
- Textarea: `bg-background-elevated text-text border border-background-subtle`
- Placeholder: `text-text-subtle`
- Character count: `text-text-subtle`
- Replace `bg-white` → `bg-background-surface`
- Replace `text-gray-*` → appropriate variants

**DevFeedbackBoxes.tsx** (~23 old refs): Developer feedback display.
- Container: `bg-background-surface`
- Feedback categories: use status colors for different severity levels
- Replace `text-green-*` → `text-status-success`
- Replace `text-red-*` → `text-status-error`
- Replace `text-yellow-*`/`text-amber-*` → `text-status-warning`
- Replace `bg-green-*` → `bg-status-success/10`
- Replace `bg-red-*` → `bg-status-error/10`
- Replace all `bg-white` / `bg-gray-*` → `bg-background-surface` / `bg-background-elevated`
- Replace all `text-gray-*` → appropriate variants

**AudioQualityWarning.tsx** (~5 old refs with amber colors): Audio quality alert.
- Replace `bg-amber-50` → `bg-status-warning/10`
- Replace `text-amber-600`, `text-amber-700`, `text-amber-900` → `text-status-warning`
- Replace `border-amber-200` → `border-status-warning/30`
- Replace `hover:bg-amber-100` → `hover:bg-status-warning/20`

**src/types/technique.ts** (~2 old refs): Difficulty level color mappings.
- Replace `text-green-700` → `text-status-success`
- Replace `bg-green-100` → `bg-status-success/15`
- Replace `text-amber-700` → `text-status-warning`
- Replace `bg-amber-100` → `bg-status-warning/15`
- Replace `text-red-700` → `text-status-error`
- Replace `bg-red-100` → `bg-status-error/15`
  </action>
  <verify>Grep all 5 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `text-red`, `bg-red`, `text-green`, `bg-green`, `text-amber`, `bg-amber` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>SettingsPanel, QuickNoteBox, DevFeedbackBoxes, AudioQualityWarning, and technique.ts contain zero old palette references. All status-colored elements use semantic status tokens. Difficulty levels use status-success/warning/error.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Grep all 14 files for old palette references — should find zero matches
3. All files use only semantic tokens from design system
4. Difficulty level colors in technique.ts use semantic status-* tokens
</verification>

<success_criteria>
- 14 files fully migrated from clinical/light palette to dark premium semantic tokens
- Zero clinical-* references in migrated files
- Zero hardcoded gray/white/black/amber/green/red Tailwind classes in migrated files
- WelcomeScreen sets premium tone with gold CTA
- Status components (BrowserWarning, AudioQualityWarning, MicPermissionError) use semantic status colors
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-color-migration/12-04-SUMMARY.md`
</output>
