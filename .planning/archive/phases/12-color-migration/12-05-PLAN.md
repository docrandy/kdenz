---
phase: 12-color-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/Settings.tsx
  - src/pages/Privacy.tsx
  - src/pages/PostSessionResults.tsx
  - src/pages/BaselineResults.tsx
  - src/pages/BaselineSession.tsx
  - src/pages/PreSessionScreen.tsx
  - src/pages/EvaluationPage.tsx
  - src/pages/ScenarioLibrary.tsx
  - src/pages/ScenarioDetail.tsx
  - src/pages/SessionDetail.tsx
  - src/pages/TechniqueFeedback.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard renders on dark navy background with proper section cards"
    - "Settings page forms render on dark surfaces with readable inputs"
    - "Privacy page renders all text content in cream on dark background"
    - "PostSessionResults displays metrics and scores on dark surfaces"
    - "ScenarioLibrary shows scenario cards on dark surfaces with gold accent for active elements"
    - "ScenarioDetail renders content on dark surface with proper text hierarchy"
    - "SessionDetail displays historical session data on dark surface"
    - "TechniqueFeedback renders AI feedback on dark surface with status colors"
    - "All pages use consistent dark premium palette — no white/gray remnants"
  artifacts:
    - path: "src/pages/Dashboard.tsx"
      provides: "Dark-themed main dashboard"
      contains: "bg-background"
    - path: "src/pages/Settings.tsx"
      provides: "Dark-themed settings page"
      contains: "bg-background"
    - path: "src/pages/Privacy.tsx"
      provides: "Dark-themed privacy page"
      contains: "bg-background"
    - path: "src/pages/ScenarioLibrary.tsx"
      provides: "Dark-themed scenario listing"
      contains: "bg-background"
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "tailwind.config.js"
      via: "semantic color tokens"
      pattern: "bg-background|text-text|bg-background-surface"
---

<objective>
Migrate all 12 page components from clinical light palette to dark premium palette.

Purpose: Pages are full-screen layouts containing multiple child components. They define the background context and structural layout within which child components render. Dashboard (40 refs) and Settings (36 refs) are the heaviest. ScenarioDetail (33 refs), TechniqueFeedback (35 refs), SessionDetail (29 refs), and ScenarioLibrary (24 refs) are technique-feature pages. Privacy (32 refs) is text-heavy. These collectively define the visual foundation every user sees.

Output: 12 page files migrated from clinical/white/gray/teal to dark premium semantic tokens.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-color-migration/12-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md
@.planning/phases/11-design-system-foundation/11-02-SUMMARY.md
@tailwind.config.js
@src/index.css
</context>

<migration_reference>
## Color Migration Mapping (apply to ALL files in this plan)

### Background replacements
- `bg-white` → `bg-background` (page-level) or `bg-background-surface` (card/panel)
- `bg-gray-50`, `bg-gray-100` → `bg-background-surface` or `bg-background-elevated`
- `bg-gray-200`, `bg-gray-300` → `bg-background-elevated`
- `bg-clinical-bg` → `bg-background`
- `bg-clinical-accent` → `bg-accent`

### Text replacements
- `text-gray-900`, `text-clinical-text`, `text-black` → `text-text`
- `text-gray-700`, `text-gray-600` → `text-text-muted`
- `text-gray-500`, `text-gray-400`, `text-clinical-muted` → `text-text-subtle`
- `text-white` (on dark bg) → `text-text`; (on accent bg) → `text-text-inverse`
- `text-clinical-accent` → `text-accent`

### Border replacements
- `border-gray-200`, `border-gray-300`, `border-clinical-border` → `border-background-elevated`
- `border-gray-100` → `border-background-surface`

### Status color replacements
- `text-red-*`, `bg-red-*` → `text-status-error`, `bg-status-error/10`
- `text-green-*`, `bg-green-*` → `text-status-success`, `bg-status-success/10`
- `text-blue-*`, `bg-blue-*` → `text-status-info`, `bg-status-info/10`
- `text-yellow-*`, `text-amber-*` → `text-status-warning`, `bg-status-warning/10`

### Interactive states
- `hover:bg-gray-50`, `hover:bg-gray-100` → `hover:bg-background-elevated`
- `hover:bg-gray-200` → `hover:bg-background-subtle`
- `focus:ring-blue-*` → `focus:ring-accent`
- `divide-gray-*` → `divide-background-elevated`

### Per CONTEXT decisions (pages)
- Page backgrounds: `bg-background` (dark navy) — body already has this via base styles, but pages often add their own bg
- Section cards/panels: `bg-background-surface border border-background-elevated`
- Tables/lists: `divide-background-elevated` for row separators
- Nav breadcrumbs: `text-text-subtle` with `hover:text-text`
- Empty states: `text-text-subtle` with `bg-background-surface` container
</migration_reference>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Dashboard and Settings pages</name>
  <files>src/pages/Dashboard.tsx, src/pages/Settings.tsx</files>
  <action>
These are the two heaviest pages (~76 combined refs). Read each file fully and apply migration.

**Dashboard.tsx** (~40 old refs): Main hub page.
- Page wrapper: `bg-background` (may already inherit from body)
- Section cards (profile preview, practice modules, session list): `bg-background-surface border border-background-elevated rounded-xl p-6`
- Section headings: `text-text`
- Section subtext: `text-text-muted`
- Session list items: `bg-background-elevated` with `hover:bg-background-subtle`
- Activity heatmap container: `bg-background-surface`
- Links/navigation: `text-accent hover:text-accent-hover` for primary; `text-text-muted hover:text-text` for secondary
- Replace ALL `bg-white` → `bg-background` or `bg-background-surface`
- Replace ALL `text-gray-*` → appropriate `text-text-*` variant
- Replace ALL `border-gray-*` → `border-background-elevated`
- Replace ALL `divide-gray-*` → `divide-background-elevated`
- Replace ALL `bg-gray-*` → appropriate `bg-background-*` variant
- Replace ALL `hover:bg-gray-*` → `hover:bg-background-elevated` or `hover:bg-background-subtle`
- CTA buttons: `btn-primary` for main action, `btn-secondary` or `btn-ghost` for others

**Settings.tsx** (~36 old refs): Settings/preferences page.
- Page wrapper: `bg-background`
- Settings sections: `bg-background-surface border border-background-elevated rounded-xl p-6`
- Section headings: `text-text`
- Form labels: `text-text-muted`
- Input fields: `bg-background-elevated border border-background-subtle text-text` (or use `input` class)
- Select dropdowns: same as input fields
- Toggle switches: active = `bg-accent`, inactive = `bg-background-elevated`
- Destructive buttons (clear data): `bg-status-error/10 text-status-error hover:bg-status-error/20`
- Save button: `btn-primary`
- Replace ALL `bg-white` / `bg-gray-*` / `border-gray-*` / `text-gray-*` / `clinical-*`
- This has 36 references — be very thorough
  </action>
  <verify>Grep both files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `divide-gray`, `text-black` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>Dashboard and Settings contain zero old palette references. Dashboard uses dark surface cards with proper text hierarchy. Settings uses proper dark form controls with gold CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Privacy, PostSessionResults, BaselineResults, BaselineSession</name>
  <files>src/pages/Privacy.tsx, src/pages/PostSessionResults.tsx, src/pages/BaselineResults.tsx, src/pages/BaselineSession.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**Privacy.tsx** (~32 old refs): Privacy transparency page — text-heavy.
- Page wrapper: `bg-background`
- Content container: `bg-background-surface border border-background-elevated rounded-xl p-8`
- Page title: `text-text`
- Section headers: `text-text`
- Body paragraphs: `text-text-muted` (cream-muted for long-form readability)
- Bullet lists: `text-text-muted`
- Emphasis text: `text-text` (brighter cream for emphasis)
- Links: `text-accent hover:text-accent-hover`
- Replace ALL `bg-white` / `text-gray-*` / `text-black` / `text-clinical-*` / `clinical-*`
- This has 32 references — many are text-color classes

**PostSessionResults.tsx** (~24 old refs): Post-session results page.
- Page wrapper: `bg-background`
- Results cards: `bg-background-surface border border-background-elevated`
- Score displays: `text-text` for primary values, status colors for indicators
- Section labels: `text-text-muted`
- Action buttons: `btn-primary` for "Practice Again", `btn-ghost` for "Back to Dashboard"
- Replace all old palette references

**BaselineResults.tsx** (~16 old refs): Baseline measurement results.
- Page wrapper: `bg-background`
- Results container: `bg-background-surface`
- Baseline metrics: `text-text` for values, `text-text-muted` for labels
- Comparison indicators (if any): use status colors
- Replace all old palette references

**BaselineSession.tsx** (~8 old refs): Baseline recording session.
- Page wrapper: `bg-background`
- Instructions: `text-text-muted`
- Recording indicator: use `bg-accent` or `bg-status-error` for recording state
- Replace all old palette references
  </action>
  <verify>Grep all 4 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `text-black` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>Privacy, PostSessionResults, BaselineResults, and BaselineSession contain zero old palette references. Privacy page readable with cream text on dark. PostSessionResults uses gold CTA for practice-again flow.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate PreSessionScreen, EvaluationPage, ScenarioLibrary, ScenarioDetail, SessionDetail, TechniqueFeedback</name>
  <files>src/pages/PreSessionScreen.tsx, src/pages/EvaluationPage.tsx, src/pages/ScenarioLibrary.tsx, src/pages/ScenarioDetail.tsx, src/pages/SessionDetail.tsx, src/pages/TechniqueFeedback.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**PreSessionScreen.tsx** (~6 old refs): Pre-session setup page.
- Page wrapper: `bg-background`
- Setup options: `bg-background-surface border border-background-elevated`
- Start button: `btn-primary` (gold CTA)
- Replace all old palette references

**EvaluationPage.tsx** (~7 old refs): Session evaluation page.
- Page wrapper: `bg-background`
- Evaluation sections: `bg-background-surface`
- Rating elements: gold for selected, `bg-background-elevated` for unselected
- Replace all old palette references

**ScenarioLibrary.tsx** (~24 old refs): Technique scenario listing.
- Page wrapper: `bg-background`
- Scenario cards: `bg-background-surface border border-background-elevated hover:border-accent/20`
- Card titles: `text-text`
- Card descriptions: `text-text-muted`
- Difficulty badges: use `badge-success` / `badge-warning` / `badge-error` classes (or status-* tokens)
- Category filters: `bg-background-elevated text-text-muted` → active: `bg-accent text-text-inverse`
- Replace ALL `bg-white` / `bg-gray-*` / `text-gray-*` / `border-gray-*`

**ScenarioDetail.tsx** (~33 old refs): Individual scenario page.
- Page wrapper: `bg-background`
- Scenario content card: `bg-background-surface border border-background-elevated`
- Title: `text-text`
- Description/context: `text-text-muted`
- Tips/instructions: `bg-background-elevated border border-background-subtle`
- Start button: `btn-primary`
- Back link: `text-text-muted hover:text-text`
- Difficulty badge: use semantic status badge
- Replace ALL old palette references (33 is significant)

**SessionDetail.tsx** (~29 old refs): Historical session detail page.
- Page wrapper: `bg-background`
- Session metrics cards: `bg-background-surface border border-background-elevated`
- Metric values: `text-text`
- Metric labels: `text-text-muted`
- Transcript section: `bg-background-surface`
- Timeline/playback: handled by child components (already in Plan 02/03)
- Replace ALL old palette references

**TechniqueFeedback.tsx** (~35 old refs): AI technique feedback page.
- Page wrapper: `bg-background`
- Feedback sections: `bg-background-surface border border-background-elevated`
- AI feedback text: `text-text-muted`
- Score displays: status colors for feedback quality indicators
- Suggestions: `bg-background-elevated text-text-muted`
- "Try Again" CTA: `btn-primary`
- Replace ALL old palette references (35 is significant)
  </action>
  <verify>Grep all 6 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `text-black`, `bg-green`, `text-green` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>PreSessionScreen, EvaluationPage, ScenarioLibrary, ScenarioDetail, SessionDetail, and TechniqueFeedback contain zero old palette references. All pages use consistent dark premium palette with proper text hierarchy and gold CTAs.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` passes with zero errors
3. Grep all 12 page files for old palette references — should find zero matches
4. All pages use consistent semantic tokens
</verification>

<success_criteria>
- 12 page files fully migrated from clinical/light palette to dark premium semantic tokens
- Zero clinical-* references in migrated files
- Zero hardcoded gray/white/black Tailwind classes in migrated files
- All pages render on dark navy background with cream text
- Cards and panels use bg-background-surface with border depth
- Gold accent reserved for primary CTAs only
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-color-migration/12-05-SUMMARY.md`
</output>
