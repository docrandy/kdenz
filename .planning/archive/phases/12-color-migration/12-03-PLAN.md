---
phase: 12-color-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FillerGauge.tsx
  - src/components/MetricCard.tsx
  - src/components/Scorecard.tsx
  - src/components/AISummary.tsx
  - src/components/HighlightToggle.tsx
  - src/components/WeeklyTrendChart.tsx
  - src/components/ContributionHeatmap.tsx
  - src/components/TranscriptView.tsx
  - src/components/TranscriptConfidenceIndicator.tsx
autonomous: true

must_haves:
  truths:
    - "MetricCard renders on dark surface with cream text and gold accent for key metrics"
    - "Scorecard displays session results on dark background with semantic status colors"
    - "AISummary panel renders on dark surface with proper text hierarchy"
    - "WeeklyTrendChart displays with colors visible on dark navy background"
    - "TranscriptView renders transcript text in cream on dark surface"
    - "FillerGauge uses gold accent for gauge fill on dark background"
    - "HighlightToggle uses dark surfaces with gold-selected state"
  artifacts:
    - path: "src/components/MetricCard.tsx"
      provides: "Dark-themed metric display card"
      contains: "bg-background-surface"
    - path: "src/components/AISummary.tsx"
      provides: "Dark-themed AI summary panel"
      contains: "bg-background"
    - path: "src/components/TranscriptView.tsx"
      provides: "Dark-themed transcript display"
      contains: "text-text"
    - path: "src/components/WeeklyTrendChart.tsx"
      provides: "Chart with dark-appropriate colors"
      contains: "bg-background"
  key_links:
    - from: "src/components/MetricCard.tsx"
      to: "tailwind.config.js"
      via: "semantic color tokens"
      pattern: "bg-background-surface|text-text"
    - from: "src/components/Scorecard.tsx"
      to: "src/components/MetricCard.tsx"
      via: "composition"
      pattern: "MetricCard"
---

<objective>
Migrate 9 feedback, metrics, and transcript components from clinical light palette to dark premium palette.

Purpose: These components display session results and feedback — MetricCard, Scorecard, AISummary, charts, and transcript views. They are data-heavy and need careful color treatment to maintain readability on dark backgrounds. Score colors need to balance quick readability with the muted premium aesthetic (per CONTEXT: may be slightly more vivid than general status).

Output: 9 files migrated from clinical/white/gray/teal to dark premium semantic tokens.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-color-migration/12-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md
@.planning/phases/11-design-system-foundation/11-02-SUMMARY.md
@tailwind.config.js
@src/index.css
</context>

<migration_reference>
## Color Migration Mapping (apply to ALL files in this plan)

### Background replacements
- `bg-white` → `bg-background` (page-level) or `bg-background-surface` (card/panel)
- `bg-gray-50`, `bg-gray-100` → `bg-background-surface` or `bg-background-elevated`
- `bg-gray-200`, `bg-gray-300` → `bg-background-elevated`
- `bg-clinical-bg` → `bg-background`
- `bg-clinical-accent` → `bg-accent`

### Text replacements
- `text-gray-900`, `text-clinical-text`, `text-black` → `text-text`
- `text-gray-700`, `text-gray-600` → `text-text-muted`
- `text-gray-500`, `text-gray-400`, `text-clinical-muted` → `text-text-subtle`
- `text-white` (on dark bg) → `text-text`; (on accent bg) → `text-text-inverse`
- `text-clinical-accent` → `text-accent`

### Border replacements
- `border-gray-200`, `border-gray-300`, `border-clinical-border` → `border-background-elevated`
- `border-gray-100` → `border-background-surface`

### Status color replacements
- `text-red-*`, `bg-red-*` → `text-status-error`, `bg-status-error/10`
- `text-green-*`, `bg-green-*` → `text-status-success`, `bg-status-success/10`
- `text-blue-*`, `bg-blue-*` → `text-status-info`, `bg-status-info/10`
- `text-yellow-*`, `text-amber-*` → `text-status-warning`, `bg-status-warning/10`

### Interactive states
- `hover:bg-gray-50`, `hover:bg-gray-100` → `hover:bg-background-elevated`
- `hover:bg-gray-200` → `hover:bg-background-subtle`
- `focus:ring-blue-*` → `focus:ring-accent`

### Per CONTEXT decisions (specific to this plan)
- **Scorecard score colors:** May be slightly more vivid than general status for quick readability. Use status colors at full opacity for score text (not the /15 tinted variant).
- **Filler word highlight treatment:** Gold underline or `bg-status-warning/20` tint — visible but not jarring on dark.
- **Card/panel depth:** Use `card-surface` class for metric cards and panels, with `border border-background-elevated` for depth.
- **Chart colors:** Ensure chart lines/fills are visible on dark. Use gold for primary metric, cream for secondary, status colors for comparison.
</migration_reference>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FillerGauge, MetricCard, Scorecard, AISummary, HighlightToggle</name>
  <files>src/components/FillerGauge.tsx, src/components/MetricCard.tsx, src/components/Scorecard.tsx, src/components/AISummary.tsx, src/components/HighlightToggle.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**FillerGauge.tsx** (~4 old refs): Real-time filler count gauge.
- Replace `bg-clinical-accent`/`#00D4FF` gauge fill → `bg-accent` (gold fill for the gauge — primary visual)
- Replace `text-clinical-*` → `text-text`
- Background: `bg-background-surface`
- The gauge should feel premium — gold fill on dark surface

**MetricCard.tsx** (~12 old refs): Reusable metric display card.
- This file is heavily used — needs careful treatment
- Card container: use `card-surface` class (or `bg-background-surface border border-background-elevated rounded-xl p-6`)
- Metric value text: `text-text` (cream, large/bold)
- Metric label: `text-text-muted`
- Replace all `bg-white`/`bg-gray-*` → `bg-background-surface`
- Replace all `text-gray-*` → appropriate `text-text-*` variant
- Replace `border-gray-*` → `border-background-elevated`
- Score color indicators: keep readable — use status colors at full text opacity

**Scorecard.tsx** (~3 old refs): Post-session results display.
- Replace `bg-white` → `bg-background`
- Replace `text-clinical-*` → `text-text`
- Section headers: `text-text-muted` for labels
- Score values: `text-text` for primary, status colors for good/bad indicators

**AISummary.tsx** (~17 old refs): AI-generated session summary panel.
- Replace all `bg-white`/`bg-gray-*` → `bg-background-surface`
- Replace `text-gray-*` → `text-text`/`text-text-muted`
- Replace `clinical-*` → semantic equivalents
- "Generate Summary" button: `btn-primary` (gold CTA) or `bg-accent text-text-inverse`
- Loading state: `text-text-subtle` with `bg-background-elevated`
- Summary text content: `text-text` for readability
- Replace `border-gray-*` → `border-background-elevated`

**HighlightToggle.tsx** (~4 old refs): Toggle between pace/filler highlights.
- Selected toggle: `bg-accent text-text-inverse` (gold selected state)
- Unselected toggle: `bg-background-elevated text-text-muted`
- Container: `bg-background-surface`
- Replace `text-clinical-*` → `text-text`
  </action>
  <verify>Grep all 5 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `#00D4FF` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>FillerGauge, MetricCard, Scorecard, AISummary, and HighlightToggle contain zero old palette references. MetricCard uses card-surface pattern. AISummary uses gold CTA for generate button. HighlightToggle uses gold for selected state.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate WeeklyTrendChart, ContributionHeatmap, TranscriptView, TranscriptConfidenceIndicator</name>
  <files>src/components/WeeklyTrendChart.tsx, src/components/ContributionHeatmap.tsx, src/components/TranscriptView.tsx, src/components/TranscriptConfidenceIndicator.tsx</files>
  <action>
Read each file and apply the migration mapping. Key specifics:

**WeeklyTrendChart.tsx** (~11 old refs): Chart component showing session trends.
- Replace `bg-white` container → `bg-background-surface`
- Replace `text-gray-*` → `text-text`/`text-text-muted`
- Replace `border-gray-*` → `border-background-elevated`
- Chart colors (if inline styles or chart config):
  - Primary line/fill: use `#c9a84c` (gold) for main metric
  - Axis text: use `#e8e2d6` (cream) or `#9a9484` (muted tan)
  - Grid lines: use `#1a1f2e` (navy-800) or `#222838` (navy-700) — subtle on dark
- If using Recharts/Chart.js: update color props in chart config objects

**ContributionHeatmap.tsx** (~7 old refs): Activity heatmap grid.
- Replace `bg-white` → `bg-background-surface`
- Replace `text-gray-*` → `text-text-muted`
- Heatmap cells: empty = `bg-background-elevated`, low activity = `bg-accent/20`, medium = `bg-accent/40`, high = `bg-accent/60`, max = `bg-accent` (gold intensity scale)
- Replace any green scale (`bg-green-*`) with gold intensity scale
- Replace `border-gray-*` → `border-background-elevated`

**TranscriptView.tsx** (~8 old refs): Full transcript display.
- Container: `bg-background-surface`
- Transcript text: `text-text` (cream on dark — high readability)
- Timestamps: `text-text-subtle`
- Filler word highlights: `bg-status-warning/20 text-status-warning` (visible but not jarring — per CONTEXT: dusty rose tint)
- Replace all `text-gray-*` → appropriate variants
- Replace `border-gray-*` → `border-background-elevated`

**TranscriptConfidenceIndicator.tsx** (~5 old refs): Confidence level display.
- High confidence: `text-status-success`
- Medium confidence: `text-status-warning`
- Low confidence: `text-status-error`
- Replace `text-green-*` → `text-status-success`
- Replace `text-yellow-*`/`text-amber-*` → `text-status-warning`
- Replace `text-red-*` → `text-status-error`
- Replace `bg-gray-*` → `bg-background-elevated`
  </action>
  <verify>Grep all 4 files for `clinical-`, `bg-white`, `text-gray`, `border-gray`, `bg-green`, `text-green`, `bg-red`, `text-red` — should find zero. Run `npx tsc --noEmit`.</verify>
  <done>WeeklyTrendChart, ContributionHeatmap, TranscriptView, and TranscriptConfidenceIndicator contain zero old palette references. Charts use gold/cream/navy for dark-appropriate colors. Heatmap uses gold intensity scale. Transcript highlights use status-warning tint.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "clinical-\|bg-white\|text-gray\|border-gray\|bg-green\|text-green\|bg-red\|text-red\|#00D4FF" src/components/FillerGauge.tsx src/components/MetricCard.tsx src/components/Scorecard.tsx src/components/AISummary.tsx src/components/HighlightToggle.tsx src/components/WeeklyTrendChart.tsx src/components/ContributionHeatmap.tsx src/components/TranscriptView.tsx src/components/TranscriptConfidenceIndicator.tsx` returns no matches
3. All 9 files use only semantic tokens from design system
</verification>

<success_criteria>
- 9 files fully migrated from clinical/light palette to dark premium semantic tokens
- Zero clinical-* references in migrated files
- Zero hardcoded gray/white/black Tailwind classes in migrated files
- Charts and heatmap use dark-appropriate colors (gold primary, cream secondary)
- Score and confidence indicators use semantic status colors
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-color-migration/12-03-SUMMARY.md`
</output>
