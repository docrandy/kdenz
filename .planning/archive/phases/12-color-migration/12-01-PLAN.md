---
phase: 12-color-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/ErrorBoundary.tsx
  - src/components/LoadingSpinner.tsx
  - src/components/SessionOrb.tsx
  - src/components/SessionProgressBar.tsx
autonomous: true

must_haves:
  truths:
    - "App shell renders dark navy background (#0b0e14) with cream text"
    - "SessionOrb uses gold accent color instead of Signal Green"
    - "ErrorBoundary fallback renders on dark background with semantic tokens"
    - "LoadingSpinner uses gold accent color"
    - "SessionProgressBar uses gold accent on dark background"
  artifacts:
    - path: "src/App.tsx"
      provides: "Dark shell wrapper"
      contains: "bg-background text-text"
    - path: "src/components/SessionOrb.tsx"
      provides: "Gold-themed recording orb"
      contains: "color-gold"
    - path: "src/components/ErrorBoundary.tsx"
      provides: "Dark-themed error fallback"
      contains: "bg-background"
    - path: "src/components/LoadingSpinner.tsx"
      provides: "Gold-themed spinner"
      contains: "accent"
    - path: "src/components/SessionProgressBar.tsx"
      provides: "Gold progress bar on dark"
      contains: "accent"
  key_links:
    - from: "src/App.tsx"
      to: "tailwind.config.js"
      via: "semantic color tokens"
      pattern: "bg-background|text-text"
    - from: "src/components/SessionOrb.tsx"
      to: "src/index.css"
      via: "CSS custom properties for gold"
      pattern: "color-gold"
---

<objective>
Migrate the app shell (App.tsx) and 4 core shared components from clinical light palette to dark premium palette using Phase 11 semantic tokens.

Purpose: App.tsx wraps the entire application — migrating it first establishes the dark navy background that all other components will render against. SessionOrb is the most visually prominent component and shifts from Signal Green to gold. ErrorBoundary, LoadingSpinner, and SessionProgressBar are shared infrastructure used across multiple pages.

Output: 5 files migrated from clinical/white/gray/teal to dark premium semantic tokens.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-color-migration/12-CONTEXT.md
@.planning/phases/11-design-system-foundation/11-01-SUMMARY.md
@.planning/phases/11-design-system-foundation/11-02-SUMMARY.md
@tailwind.config.js
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate App.tsx shell and back-nav headers to dark premium</name>
  <files>src/App.tsx</files>
  <action>
Replace all old color classes in App.tsx with semantic tokens:

1. **Root wrappers** (2 instances): Replace `bg-clinical-bg text-clinical-text` with `bg-background text-text`
2. **Back-nav headers** (FreePracticeFillerRoute, FreePracticePaceRoute, TechniquePracticeRoute):
   - `bg-white border-b` → `bg-background-surface border-b border-background-elevated`
   - `text-gray-600 hover:text-gray-900` → `text-text-muted hover:text-text`

These back-nav headers appear 3 times with identical patterns. All 3 should be migrated identically.

No functional changes — purely visual token migration.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Grep src/App.tsx for any remaining `clinical-`, `bg-white`, `text-gray` — should find zero matches.</verify>
  <done>App.tsx contains zero references to clinical palette, bg-white, text-gray, or text-black. Uses only semantic tokens (bg-background, text-text, bg-background-surface, text-text-muted, border-background-elevated).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SessionOrb from Signal Green to gold accent</name>
  <files>src/components/SessionOrb.tsx</files>
  <action>
SessionOrb uses inline styles with hardcoded Signal Green (#00C851). Migrate to gold accent:

1. **Base color**: Change `const baseColor = '#00C851'` to use CSS variable `getComputedStyle(document.documentElement).getPropertyValue('--color-gold-500').trim()` or hardcode `#c9a84c` (simpler, consistent with prototype). Use `#c9a84c`.
2. **Glow color**: Change `'rgba(0, 200, 81, OPACITY)'` to `'rgba(201, 168, 76, OPACITY)'` (warm gold glow — candlelight feel per CONTEXT decisions, NOT electric/neon).
3. **SVG icon fills** (WaveformBarsIcon bars): Change `fill="#000000"` to `fill="#0b0e14"` (dark navy — visible on gold orb).
4. **Stop icon**: Change `fill="#EF4444"` (red) to `fill="#0b0e14"` (dark navy square on gold — matches premium aesthetic, or use cream `#e8e2d6` for contrast). Use `#e8e2d6` cream for the stop square.
5. **Comment update**: Change "Signal Green color from locked design system" comment to "Gold accent from v2.0 design system".

Per CONTEXT decisions: Gold is warm & luxurious, candlelight feel. The glow should be soft gold radiance, not electric.
  </action>
  <verify>Grep SessionOrb.tsx for `#00C851`, `rgba(0, 200, 81` — should find zero. Grep for `#c9a84c` or `gold` to confirm migration. Run `npx tsc --noEmit`.</verify>
  <done>SessionOrb uses gold (#c9a84c) as base color with warm gold glow (rgba 201,168,76). No Signal Green references remain. SVG icons use dark navy/cream fills appropriate for gold background.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate ErrorBoundary, LoadingSpinner, SessionProgressBar</name>
  <files>src/components/ErrorBoundary.tsx, src/components/LoadingSpinner.tsx, src/components/SessionProgressBar.tsx</files>
  <action>
**ErrorBoundary.tsx** — Contains clinical-* references and gray classes in fallback UI:
- Replace `bg-white` → `bg-background-surface`
- Replace `text-clinical-text` / `text-gray-900` → `text-text`
- Replace `text-red-600` / `text-red-500` → `text-status-error`
- Replace `bg-red-50` → `bg-status-error/10`
- Replace `text-clinical-muted` / `text-gray-500` → `text-text-muted`
- Replace `border-clinical-border` / `border-gray-200` → `border-background-elevated`
- Replace `bg-clinical-text text-white` (buttons) → `btn-primary` class OR `bg-accent text-text-inverse`
- Replace `bg-gray-100 hover:bg-gray-200` → `bg-background-elevated hover:bg-background-subtle`

**LoadingSpinner.tsx** — Contains clinical-accent reference:
- Replace `border-clinical-accent` → `border-accent`
- Any `border-gray-*` → `border-background-elevated`

**SessionProgressBar.tsx** — Contains clinical references for progress bar:
- Replace `bg-clinical-accent` → `bg-accent`
- Replace `bg-gray-200` / `bg-clinical-border` → `bg-background-elevated`
- Any `text-clinical-*` → appropriate `text-text-*` variant

For all 3 files: Remove any `bg-white` → `bg-background-surface`, `shadow-*` can stay (shadows are already mapped in design tokens).
  </action>
  <verify>Grep all 3 files for `clinical-`, `bg-white`, `text-gray`, `text-red`, `border-gray` — should find zero matches. Run `npx tsc --noEmit` and `npm run build` to confirm no breakage.</verify>
  <done>ErrorBoundary, LoadingSpinner, and SessionProgressBar contain zero clinical/gray/white color references. All use semantic tokens from the design system.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `grep -r "clinical-" src/App.tsx src/components/ErrorBoundary.tsx src/components/LoadingSpinner.tsx src/components/SessionOrb.tsx src/components/SessionProgressBar.tsx` returns no matches
3. `grep -r "bg-white\|text-gray\|border-gray\|text-black\|#00C851" src/App.tsx src/components/ErrorBoundary.tsx src/components/LoadingSpinner.tsx src/components/SessionOrb.tsx src/components/SessionProgressBar.tsx` returns no matches (except possible string literals in error messages)
</verification>

<success_criteria>
- 5 files fully migrated from clinical/light palette to dark premium semantic tokens
- Zero clinical-* references in migrated files
- Zero hardcoded gray/white/black Tailwind classes in migrated files (bg-white, text-gray-*, etc.)
- SessionOrb uses gold (#c9a84c) with warm gold glow
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-color-migration/12-01-SUMMARY.md`
</output>
