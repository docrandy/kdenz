---
phase: 12-color-migration
plan: 07
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06"]
files_modified:
  - tailwind.config.js
  - src/index.css
autonomous: false

must_haves:
  truths:
    - "Zero clinical-* color references exist anywhere in src/"
    - "Zero hardcoded bg-white, text-gray-*, border-gray-*, text-black, bg-black classes in src/ component files"
    - "tailwind.config.js no longer contains the clinical.* color palette"
    - "npm run build succeeds with zero errors"
    - "Application renders correctly in browser with dark premium palette"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Clean design system tokens without legacy clinical palette"
      contains: "background"
    - path: "src/index.css"
      provides: "Design system with no legacy references"
      contains: "bg-background"
  key_links:
    - from: "tailwind.config.js"
      to: "src/**/*.tsx"
      via: "Tailwind class compilation"
      pattern: "bg-background|text-text|bg-accent"
---

<objective>
Remove the legacy clinical palette from tailwind.config.js, perform a comprehensive codebase-wide audit for any missed old color references, fix any stragglers, verify the build, and visually confirm the dark premium transformation.

Purpose: This is the cleanup and verification plan. Plans 01-06 migrated all ~57 files individually. This plan ensures nothing was missed, removes the legacy palette that is no longer needed, and includes a human-verify checkpoint for visual confirmation. CM-06 (build verified with no visual regressions) is satisfied here.

Output: Clean tailwind.config.js without clinical palette, zero old color references in codebase, verified build.
</objective>

<execution_context>
@C:\Users\randy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\randy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-color-migration/12-CONTEXT.md
@.planning/phases/12-color-migration/12-01-SUMMARY.md
@.planning/phases/12-color-migration/12-02-SUMMARY.md
@.planning/phases/12-color-migration/12-03-SUMMARY.md
@.planning/phases/12-color-migration/12-04-SUMMARY.md
@.planning/phases/12-color-migration/12-05-SUMMARY.md
@.planning/phases/12-color-migration/12-06-SUMMARY.md
@tailwind.config.js
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove clinical palette from tailwind.config.js and audit for stragglers</name>
  <files>tailwind.config.js</files>
  <action>
1. **Remove clinical palette from tailwind.config.js:**
   Delete the entire `clinical: { ... }` block from the colors section in tailwind.config.js:
   ```
   // ===== OLD PALETTE (keep for Phase 12 migration) =====
   clinical: {
     bg: "#FFFFFF",
     text: "#000000",
     accent: "#00D4FF",
     "accent-hover": "#00B8E6",
     muted: "#6B7280",
     border: "#E5E7EB",
     "deep-navy": "#1A1A2E",
     "electric-blue": "#0066FF",
     "signal-green": "#00C851",
     "warm-amber": "#FFB300",
     "soft-gray": "#F5F5F7",
   },
   ```
   Also remove the comment above it. The only colors remaining should be the semantic tokens (background, text, accent, status).

2. **Codebase-wide audit for ANY remaining old color references:**
   Run these greps across the entire src/ directory:
   - `grep -r "clinical-" src/` — should return ZERO matches
   - `grep -r "bg-white\b" src/ --include="*.tsx" --include="*.ts"` — should return ZERO (except possible modal backdrop overlays using `bg-white/` opacity which would need evaluation)
   - `grep -r "text-gray-\|bg-gray-\|border-gray-" src/ --include="*.tsx" --include="*.ts"` — should return ZERO
   - `grep -r "text-black\b\|bg-black\b" src/ --include="*.tsx" --include="*.ts"` — should return ZERO (except `bg-black/50` backdrop overlays which are fine)
   - `grep -r "#00D4FF\|#00C851" src/` — should return ZERO
   - `grep -r "text-red-\|bg-red-\|text-green-\|bg-green-\|text-blue-\|bg-blue-\|text-yellow-\|bg-yellow-\|text-amber-\|bg-amber-\|text-orange-\|bg-orange-" src/ --include="*.tsx" --include="*.ts"` — should return ZERO

3. **Fix any stragglers found:**
   If any old color references remain, migrate them using the same patterns from Plans 01-06.
   Common stragglers:
   - Inline styles with hardcoded hex colors
   - Conditional className strings that were missed
   - Template literal color values
   - Chart/graph color configuration objects

4. **Update index.css comment if needed:**
   If index.css still has any reference to "clinical" or "old palette", remove those comments.
  </action>
  <verify>
All grep commands above return zero matches (allowing only bg-black/50 backdrop overlays).
`npx tsc --noEmit` passes.
tailwind.config.js no longer contains the word "clinical".
  </verify>
  <done>Clinical palette completely removed from tailwind.config.js. Zero old color references remain in any src/ file (except acceptable bg-black/50 overlays). TypeScript compilation passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete dark premium color migration across ~57 files:
- All backgrounds migrated from white to dark navy (#0b0e14)
- All surface elements use navy surface colors (#131720, #1a1f2e)
- All text migrated from black/gray to cream/tan (#e8e2d6, #9a9484)
- All accents migrated from teal (#00D4FF) to gold (#c9a84c)
- All status colors use muted semantic palette (sage green, dusty rose, muted blue)
- Clinical palette removed from tailwind.config.js
  </what-built>
  <how-to-verify>
1. Run `npm run dev` to start the dev server
2. Open http://localhost:5173 in Chrome
3. Check these screens (each should render on dark navy background with cream text):
   - **Welcome Screen**: Dark background, gold "Start" button, cream text
   - **Dashboard**: Dark cards with cream text, gold accent on primary actions
   - **Pre-Session Screen**: Dark background, gold CTA
   - **Practice Session** (start a recording): Dark background, gold orb, cream controls
   - **Post-Session Results**: Dark metric cards with cream text
   - **Settings**: Dark form with proper input field contrast
   - **Scenario Library**: Dark scenario cards with gold hover effects
   - **Profile**: Dark form with gold accents on selected elements
4. Verify NO white backgrounds, no teal accents, no light-mode remnants
5. Verify text is readable (cream on dark navy has good contrast)
6. Verify status colors are visible but muted (sage green, dusty rose, not neon)
  </how-to-verify>
  <resume-signal>Type "approved" if the dark premium palette looks correct across all screens, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `grep -r "clinical" tailwind.config.js` returns zero matches
3. Full codebase grep for old palette references returns zero matches
4. Human visual verification passes
</verification>

<success_criteria>
- tailwind.config.js contains ONLY semantic tokens (no clinical palette)
- Zero old color references anywhere in src/
- npm run build succeeds
- Human verifies dark premium palette renders correctly across all screens
- CM-01 through CM-06 requirements all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-color-migration/12-07-SUMMARY.md`
</output>
